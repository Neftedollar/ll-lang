module Examples.Stdlib

Maybe A = Some A | None

double(x Int) = x * 2

sumList(xs List[Int]) = listFold (\acc. \x. acc + x) 0 xs

doubleAll(xs List[Int]) = listMap double xs

nameLen(name Str) = strLen name

main() =
  printfn "stdlib example"
