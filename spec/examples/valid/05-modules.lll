module Examples.Modules

import Std.List

-- using listTake and listDrop from Std.List
firstThree(xs List[Int]) =
  listTake 3 xs

restAfterTwo(xs List[Int]) =
  listDrop 2 xs

-- export
export greet(name Str) = strConcat "hello " name
