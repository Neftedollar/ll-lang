module Examples.Modules

import Std.List
import Std.Maybe

-- using imported functions
firstDoubled(xs List[Int]) =
  xs -> head -> map (\x. x * 2)

-- export
export greet(name Str) = "hello " + name
