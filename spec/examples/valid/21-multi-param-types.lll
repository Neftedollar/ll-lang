module Examples.MultiParam

type Maybe A = Some A | None
type Color = Red | Black
type RBMap K V = Leaf | Node Color (RBMap K V) K V (RBMap K V)

fn rbEmpty() = Leaf

fn rbSize(m RBMap[K][V]) =
  match m
    | Leaf -> 0
    | Node _ left _ _ right -> 1 + rbSize left + rbSize right

fn main() =
  let m = rbEmpty
  rbSize m
