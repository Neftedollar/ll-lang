module Examples.MultiParam

Maybe A = Some A | None
Color = Red | Black
RBMap K V = Leaf | Node Color (RBMap K V) K V (RBMap K V)

rbEmpty() = Leaf

rbSize(m RBMap[K][V]) =
  match m
    | Leaf -> 0
    | Node _ left _ _ right -> 1 + rbSize left + rbSize right

main() =
  m = rbEmpty
  rbSize m
