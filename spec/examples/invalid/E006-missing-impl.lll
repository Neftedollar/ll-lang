-- expect: E006
module Invalid.E006

trait Functor F =
  map(f A->B)(fa F[A]) F[B]

Box A = Box A

-- no impl Functor Box defined
doubled[F: Functor](xs F[Int]) F[Int] = map (\x. x * 2) xs

let bad = doubled (Box 5)
