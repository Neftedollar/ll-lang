// @ts-nocheck
// Generated by lllc (ll-lang TypeScript backend)

type Maybe<A> = { _tag: `Some`; _0: any } | { _tag: `None` };
const Some = <A>(_0: any): Maybe<A> => ({ _tag: `Some` as const, _0 });
const None: Maybe<any> = { _tag: `None` as const };

// --- ll-lang stdlib (TypeScript) ---


const isNone = (m: any | null) => (() => {
  if (m?._tag === `Some`) {  return false; }
  if (m?._tag === `None`) { return true; }
  throw new Error(`Non-exhaustive match`);
})();

const isSome = (m: any | null) => (() => {
  if (m?._tag === `Some`) {  return true; }
  if (m?._tag === `None`) { return false; }
  throw new Error(`Non-exhaustive match`);
})();

const check = (label: string) => (ok: boolean) => (ok ? ((_) => 0)((((s: string): void => { console.log(s) }))(((((a: string) => (b: string): string => a + b))(`OK `))(label))) : ((_) => 1)((((s: string): void => { console.log(s) }))(((((a: string) => (b: string): string => a + b))(`FAIL `))(label))));

const doubleInMaybe = (x: number) => Some((x * 2));

const __ll_main_Std_Maybe = () => ((r1) => ((ok1) => ((_) => ((r2) => ((ok2) => ((_) => ((r3) => ((ok3) => ((_) => ((d1) => ((d2) => ((_) => ((_) => 0)(((check)(`5 isSome/isNone`))(((isSome)(Some(1)) ? (isNone)(None) : false))))(((check)(`4 maybeWithDefault`))(((d1 === 7) ? (d2 === 99) : false))))(((maybeWithDefault)(99))(None)))(((maybeWithDefault)(99))(Some(7))))(((check)(`3 maybeBind Some`))(ok3)))((() => {
  if (r3?._tag === `Some`) { const v = (r3 as any)._0; return (v === 20); }
  if (r3?._tag === `None`) { return false; }
  throw new Error(`Non-exhaustive match`);
})()))(((((m: any | null) => (f: (x: any) => any | null): any | null => m !== null ? f(m) : null))(Some(10)))(doubleInMaybe)))(((check)(`2 maybeMap None`))(ok2)))((() => {
  if (r2?._tag === `Some`) {  return false; }
  if (r2?._tag === `None`) { return true; }
  throw new Error(`Non-exhaustive match`);
})()))(((((f: (x: any) => any) => (m: any | null): any | null => m !== null ? f(m) : null))((x: number) => (x + 1)))(None)))(((check)(`1 maybeMap Some`))(ok1)))((() => {
  if (r1?._tag === `Some`) { const v = (r1 as any)._0; return (v === 42); }
  if (r1?._tag === `None`) { return false; }
  throw new Error(`Non-exhaustive match`);
})()))(((((f: (x: any) => any) => (m: any | null): any | null => m !== null ? f(m) : null))((x: number) => (x + 1)))(Some(41)));

type Token = { _tag: `KwLet` } | { _tag: `KwTag` } | { _tag: `KwUnit` } | { _tag: `KwTrait` } | { _tag: `KwImpl` } | { _tag: `KwImport` } | { _tag: `KwExport` } | { _tag: `KwModule` } | { _tag: `KwIf` } | { _tag: `KwElse` } | { _tag: `KwTrue` } | { _tag: `KwFalse` } | { _tag: `KwMatch` } | { _tag: `Ident`; _0: string } | { _tag: `TypeId`; _0: string } | { _tag: `IntLit`; _0: number } | { _tag: `FloatLit`; _0: string } | { _tag: `StrLit`; _0: string } | { _tag: `CharLit`; _0: string } | { _tag: `Arrow` } | { _tag: `Backslash` } | { _tag: `Dot` } | { _tag: `Comma` } | { _tag: `Colon` } | { _tag: `ColonColon` } | { _tag: `Eq` } | { _tag: `Bar` } | { _tag: `LBrack` } | { _tag: `RBrack` } | { _tag: `LParen` } | { _tag: `RParen` } | { _tag: `Plus` } | { _tag: `Minus` } | { _tag: `Star` } | { _tag: `Slash` } | { _tag: `Caret` } | { _tag: `Lt` } | { _tag: `Gt` } | { _tag: `Le` } | { _tag: `Ge` } | { _tag: `EqEq` } | { _tag: `Neq` } | { _tag: `Underscore` } | { _tag: `Newline` } | { _tag: `Indent` } | { _tag: `Dedent` } | { _tag: `Eof` } | { _tag: `TError`; _0: string };
const KwLet: Token = { _tag: `KwLet` as const };
const KwTag: Token = { _tag: `KwTag` as const };
const KwUnit: Token = { _tag: `KwUnit` as const };
const KwTrait: Token = { _tag: `KwTrait` as const };
const KwImpl: Token = { _tag: `KwImpl` as const };
const KwImport: Token = { _tag: `KwImport` as const };
const KwExport: Token = { _tag: `KwExport` as const };
const KwModule: Token = { _tag: `KwModule` as const };
const KwIf: Token = { _tag: `KwIf` as const };
const KwElse: Token = { _tag: `KwElse` as const };
const KwTrue: Token = { _tag: `KwTrue` as const };
const KwFalse: Token = { _tag: `KwFalse` as const };
const KwMatch: Token = { _tag: `KwMatch` as const };
const Ident = (_0: string): Token => ({ _tag: `Ident` as const, _0 });
const TypeId = (_0: string): Token => ({ _tag: `TypeId` as const, _0 });
const IntLit = (_0: number): Token => ({ _tag: `IntLit` as const, _0 });
const FloatLit = (_0: string): Token => ({ _tag: `FloatLit` as const, _0 });
const StrLit = (_0: string): Token => ({ _tag: `StrLit` as const, _0 });
const CharLit = (_0: string): Token => ({ _tag: `CharLit` as const, _0 });
const Arrow: Token = { _tag: `Arrow` as const };
const Backslash: Token = { _tag: `Backslash` as const };
const Dot: Token = { _tag: `Dot` as const };
const Comma: Token = { _tag: `Comma` as const };
const Colon: Token = { _tag: `Colon` as const };
const ColonColon: Token = { _tag: `ColonColon` as const };
const Eq: Token = { _tag: `Eq` as const };
const Bar: Token = { _tag: `Bar` as const };
const LBrack: Token = { _tag: `LBrack` as const };
const RBrack: Token = { _tag: `RBrack` as const };
const LParen: Token = { _tag: `LParen` as const };
const RParen: Token = { _tag: `RParen` as const };
const Plus: Token = { _tag: `Plus` as const };
const Minus: Token = { _tag: `Minus` as const };
const Star: Token = { _tag: `Star` as const };
const Slash: Token = { _tag: `Slash` as const };
const Caret: Token = { _tag: `Caret` as const };
const Lt: Token = { _tag: `Lt` as const };
const Gt: Token = { _tag: `Gt` as const };
const Le: Token = { _tag: `Le` as const };
const Ge: Token = { _tag: `Ge` as const };
const EqEq: Token = { _tag: `EqEq` as const };
const Neq: Token = { _tag: `Neq` as const };
const Underscore: Token = { _tag: `Underscore` as const };
const Newline: Token = { _tag: `Newline` as const };
const Indent: Token = { _tag: `Indent` as const };
const Dedent: Token = { _tag: `Dedent` as const };
const Eof: Token = { _tag: `Eof` as const };
const TError = (_0: string): Token => ({ _tag: `TError` as const, _0 });

const isUpperChar = (c: string) => ((n) => ((n < 65) ? false : ((n > 90) ? false : true)))((((c: string): number => c.charCodeAt(0)))(c));

const isLowerChar = (c: string) => ((n) => ((n < 97) ? false : ((n > 122) ? false : true)))((((c: string): number => c.charCodeAt(0)))(c));

const isIdStart = (c: string) => ((isUpperChar)(c) ? true : (isLowerChar)(c));

const isIdCont = (c: string) => ((isIdStart)(c) ? true : ((((c: string): boolean => /\d/.test(c)))(c) ? true : (c === `_`)));

const takeIdCont = (cs: string[]) => (() => {
  if (cs.length > 0) { const c = cs[0], rest = cs.slice(1); return ((isIdCont)(c) ? ((((xs: any[]) => (ys: any[]): any[] => [...xs, ...ys]))([c]))((takeIdCont)(rest)) : []); }
  return [];
  throw new Error(`Non-exhaustive match`);
})();

const dropIdCont = (cs: string[]) => (() => {
  if (cs.length > 0) { const c = cs[0], rest = cs.slice(1); return ((isIdCont)(c) ? (dropIdCont)(rest) : cs); }
  return [];
  throw new Error(`Non-exhaustive match`);
})();

const takeDigit = (cs: string[]) => (() => {
  if (cs.length > 0) { const c = cs[0], rest = cs.slice(1); return ((((c: string): boolean => /\d/.test(c)))(c) ? ((((xs: any[]) => (ys: any[]): any[] => [...xs, ...ys]))([c]))((takeDigit)(rest)) : []); }
  return [];
  throw new Error(`Non-exhaustive match`);
})();

const dropDigit = (cs: string[]) => (() => {
  if (cs.length > 0) { const c = cs[0], rest = cs.slice(1); return ((((c: string): boolean => /\d/.test(c)))(c) ? (dropDigit)(rest) : cs); }
  return [];
  throw new Error(`Non-exhaustive match`);
})();

const classifyIdent = (s: string) => (() => {
  if (s === `let`) { return KwLet; }
  if (s === `tag`) { return KwTag; }
  if (s === `unit`) { return KwUnit; }
  if (s === `trait`) { return KwTrait; }
  if (s === `impl`) { return KwImpl; }
  if (s === `import`) { return KwImport; }
  if (s === `export`) { return KwExport; }
  if (s === `module`) { return KwModule; }
  if (s === `if`) { return KwIf; }
  if (s === `else`) { return KwElse; }
  if (s === `true`) { return KwTrue; }
  if (s === `false`) { return KwFalse; }
  if (s === `match`) { return KwMatch; }
  return ((cs) => (() => {
  return ((isUpperChar)(c) ? TypeId(s) : Ident(s));
  return Ident(s);
  throw new Error(`Non-exhaustive match`);
})())((((s: string): string[] => Array.from(s)))(s));
  throw new Error(`Non-exhaustive match`);
})();

const parseIntStr = (s: string) => (() => {
  if ((((s: string): number | null => { const n = parseInt(s, 10); return isNaN(n) ? null : n }))(s)?._tag === `Some`) { const n = ((((s: string): number | null => { const n = parseInt(s, 10); return isNaN(n) ? null : n }))(s) as any)._0; return n; }
  if ((((s: string): number | null => { const n = parseInt(s, 10); return isNaN(n) ? null : n }))(s)?._tag === `None`) { return 0; }
  throw new Error(`Non-exhaustive match`);
})();

const lexId = (cs: string[]) => ((idChars) => ((leftover) => ((tok) => ((((xs: any[]) => (ys: any[]): any[] => [...xs, ...ys]))([tok]))((lexChars)(leftover)))((classifyIdent)((((cs: string[]): string => cs.join('')))(idChars))))((dropIdCont)(cs)))((takeIdCont)(cs));

const lexNum = (cs: string[]) => ((digits) => ((rest0) => (() => {
  return ((((c: string): boolean => /\d/.test(c)))(c2) ? ((fracDigits) => ((rest2) => ((intPart) => ((fracPart) => ((floatStr) => ((((xs: any[]) => (ys: any[]): any[] => [...xs, ...ys]))([FloatLit(floatStr)]))((lexChars)(rest2)))(((((a: string) => (b: string): string => a + b))(((((a: string) => (b: string): string => a + b))(intPart))(`.`)))(fracPart)))((((cs: string[]): string => cs.join('')))(fracDigits)))((((cs: string[]): string => cs.join('')))(digits)))((dropDigit)([c2, ...rest1])))((takeDigit)([c2, ...rest1])) : ((n) => ((((xs: any[]) => (ys: any[]): any[] => [...xs, ...ys]))([IntLit(n)]))((lexChars)(rest0)))((parseIntStr)((((cs: string[]): string => cs.join('')))(digits))));
  return ((n) => ((((xs: any[]) => (ys: any[]): any[] => [...xs, ...ys]))([IntLit(n)]))((lexChars)(rest0)))((parseIntStr)((((cs: string[]): string => cs.join('')))(digits)));
  throw new Error(`Non-exhaustive match`);
})())((dropDigit)(cs)))((takeDigit)(cs));

const takeStrBody = (cs: string[]) => (() => {
  if (cs.length === 0) { return [[], []] as const; }
  if (cs.length > 0) { const c = cs[0], rest = cs.slice(1); return ((c === `"`) ? [[], rest] as const : ((c === `\\`) ? (takeStrBodyEsc)(rest) : ((pair) => (() => {
  { const body = pair[0]; const leftover = pair[1]; return [[c, ...body], leftover] as const; }
  throw new Error(`Non-exhaustive match`);
})())((takeStrBody)(rest)))); }
  throw new Error(`Non-exhaustive match`);
})();

const takeStrBodyEsc = (cs: string[]) => (() => {
  if (cs.length === 0) { return [[], []] as const; }
  if (cs.length > 0) { const esc = cs[0], rest = cs.slice(1); return ((pair) => (() => {
  { const body = pair[0]; const leftover = pair[1]; return [[(decodeEscape)(esc), ...body], leftover] as const; }
  throw new Error(`Non-exhaustive match`);
})())((takeStrBody)(rest)); }
  throw new Error(`Non-exhaustive match`);
})();

const decodeEscape = (c: string) => (() => {
  if (c === `n`) { return `\n`; }
  if (c === `t`) { return `\t`; }
  if (c === `r`) { return `\r`; }
  if (c === `\\`) { return `\\`; }
  if (c === `'`) { return `'`; }
  if (c === `"`) { return `"`; }
  if (c === `0`) { return `\0`; }
  return c;
  throw new Error(`Non-exhaustive match`);
})();

const lexStr = (cs: string[]) => ((pair) => (() => {
  { const body = pair[0]; const leftover = pair[1]; return ((((xs: any[]) => (ys: any[]): any[] => [...xs, ...ys]))([StrLit((((cs: string[]): string => cs.join('')))(body))]))((lexChars)(leftover)); }
  throw new Error(`Non-exhaustive match`);
})())((takeStrBody)(cs));

const lexCharLit = (cs: string[]) => (() => {
  if (cs.length === 0) { return [Eof]; }
  if (cs.length > 0) { const ch = cs[0], rest = cs.slice(1); return ((ch === `\\`) ? (() => {
  if (rest.length === 0) { return [Eof]; }
  if (rest.length > 0) { const esc = rest[0], rest2 = rest.slice(1); return (() => {
  return ((((xs: any[]) => (ys: any[]): any[] => [...xs, ...ys]))([CharLit((decodeEscape)(esc))]))((lexChars)(rest3));
  return (lexChars)(cs);
  throw new Error(`Non-exhaustive match`);
})(); }
  throw new Error(`Non-exhaustive match`);
})() : (() => {
  return ((((xs: any[]) => (ys: any[]): any[] => [...xs, ...ys]))([CharLit(ch)]))((lexChars)(rest2));
  return (lexChars)(cs);
  throw new Error(`Non-exhaustive match`);
})()); }
  throw new Error(`Non-exhaustive match`);
})();

const skipLineComment = (cs: string[]) => (() => {
  if (cs.length === 0) { return [Eof]; }
  if (cs.length > 0) { const c = cs[0], rest = cs.slice(1); return ((c === `\n`) ? ((((xs: any[]) => (ys: any[]): any[] => [...xs, ...ys]))([Newline]))((lexChars)(rest)) : (skipLineComment)(rest)); }
  throw new Error(`Non-exhaustive match`);
})();

const lexEqOrEqEq = (cs: string[]) => (() => {
  return ((((xs: any[]) => (ys: any[]): any[] => [...xs, ...ys]))([EqEq]))((lexChars)(rest));
  return ((((xs: any[]) => (ys: any[]): any[] => [...xs, ...ys]))([Eq]))((lexChars)(cs));
  throw new Error(`Non-exhaustive match`);
})();

const lexNeq = (cs: string[]) => (() => {
  return ((((xs: any[]) => (ys: any[]): any[] => [...xs, ...ys]))([Neq]))((lexChars)(rest));
  return (lexChars)(cs);
  throw new Error(`Non-exhaustive match`);
})();

const lexLtOrLe = (cs: string[]) => (() => {
  return ((((xs: any[]) => (ys: any[]): any[] => [...xs, ...ys]))([Le]))((lexChars)(rest));
  return ((((xs: any[]) => (ys: any[]): any[] => [...xs, ...ys]))([Lt]))((lexChars)(cs));
  throw new Error(`Non-exhaustive match`);
})();

const lexGtOrGe = (cs: string[]) => (() => {
  return ((((xs: any[]) => (ys: any[]): any[] => [...xs, ...ys]))([Ge]))((lexChars)(rest));
  return ((((xs: any[]) => (ys: any[]): any[] => [...xs, ...ys]))([Gt]))((lexChars)(cs));
  throw new Error(`Non-exhaustive match`);
})();

const lexMinusOrArrow = (cs: string[]) => (() => {
  return ((((xs: any[]) => (ys: any[]): any[] => [...xs, ...ys]))([Arrow]))((lexChars)(rest));
  return (skipLineComment)(rest);
  return ((((xs: any[]) => (ys: any[]): any[] => [...xs, ...ys]))([Minus]))((lexChars)(cs));
  throw new Error(`Non-exhaustive match`);
})();

const lexColonOrCons = (cs: string[]) => (() => {
  return ((((xs: any[]) => (ys: any[]): any[] => [...xs, ...ys]))([ColonColon]))((lexChars)(rest));
  return ((((xs: any[]) => (ys: any[]): any[] => [...xs, ...ys]))([Colon]))((lexChars)(cs));
  throw new Error(`Non-exhaustive match`);
})();

const lexUnderOrIdent = (cs: string[]) => (() => {
  return ((isIdCont)(c) ? ((idChars) => ((leftover) => ((tok) => ((((xs: any[]) => (ys: any[]): any[] => [...xs, ...ys]))([tok]))((lexChars)(leftover)))((classifyIdent)((((cs: string[]): string => cs.join('')))(idChars))))((dropIdCont)(cs)))(((((xs: any[]) => (ys: any[]): any[] => [...xs, ...ys]))([`_`]))((takeIdCont)(cs))) : ((((xs: any[]) => (ys: any[]): any[] => [...xs, ...ys]))([Underscore]))((lexChars)(cs)));
  return ((((xs: any[]) => (ys: any[]): any[] => [...xs, ...ys]))([Underscore]))((lexChars)(cs));
  throw new Error(`Non-exhaustive match`);
})();

const lexChars = (cs: string[]) => (() => {
  if (cs.length === 0) { return [Eof]; }
  if (cs.length > 0) { const c = cs[0], rest = cs.slice(1); return ((c === `\n`) ? ((((xs: any[]) => (ys: any[]): any[] => [...xs, ...ys]))([Newline]))((lexChars)(rest)) : ((((c: string): boolean => /\s/.test(c)))(c) ? (lexChars)(rest) : ((c === `-`) ? (lexMinusOrArrow)(rest) : ((isIdStart)(c) ? (lexId)(cs) : ((c === `_`) ? (lexUnderOrIdent)(rest) : ((((c: string): boolean => /\d/.test(c)))(c) ? (lexNum)(cs) : ((c === `"`) ? (lexStr)(rest) : ((c === `'`) ? (lexCharLit)(rest) : ((c === `=`) ? (lexEqOrEqEq)(rest) : ((c === `!`) ? (lexNeq)(rest) : ((c === `<`) ? (lexLtOrLe)(rest) : ((c === `>`) ? (lexGtOrGe)(rest) : ((c === `:`) ? (lexColonOrCons)(rest) : ((c === `(`) ? ((((xs: any[]) => (ys: any[]): any[] => [...xs, ...ys]))([LParen]))((lexChars)(rest)) : ((c === `)`) ? ((((xs: any[]) => (ys: any[]): any[] => [...xs, ...ys]))([RParen]))((lexChars)(rest)) : ((c === `[`) ? ((((xs: any[]) => (ys: any[]): any[] => [...xs, ...ys]))([LBrack]))((lexChars)(rest)) : ((c === `]`) ? ((((xs: any[]) => (ys: any[]): any[] => [...xs, ...ys]))([RBrack]))((lexChars)(rest)) : ((c === `|`) ? ((((xs: any[]) => (ys: any[]): any[] => [...xs, ...ys]))([Bar]))((lexChars)(rest)) : ((c === `+`) ? ((((xs: any[]) => (ys: any[]): any[] => [...xs, ...ys]))([Plus]))((lexChars)(rest)) : ((c === `*`) ? ((((xs: any[]) => (ys: any[]): any[] => [...xs, ...ys]))([Star]))((lexChars)(rest)) : ((c === `/`) ? ((((xs: any[]) => (ys: any[]): any[] => [...xs, ...ys]))([Slash]))((lexChars)(rest)) : ((c === `^`) ? ((((xs: any[]) => (ys: any[]): any[] => [...xs, ...ys]))([Caret]))((lexChars)(rest)) : ((c === `.`) ? ((((xs: any[]) => (ys: any[]): any[] => [...xs, ...ys]))([Dot]))((lexChars)(rest)) : ((c === `,`) ? ((((xs: any[]) => (ys: any[]): any[] => [...xs, ...ys]))([Comma]))((lexChars)(rest)) : ((c === `\\`) ? ((((xs: any[]) => (ys: any[]): any[] => [...xs, ...ys]))([Backslash]))((lexChars)(rest)) : ((((xs: any[]) => (ys: any[]): any[] => [...xs, ...ys]))([TError(c)]))((lexChars)(rest))))))))))))))))))))))))))); }
  throw new Error(`Non-exhaustive match`);
})();

const tokenize = (src: string) => (lexChars)((((s: string): string[] => Array.from(s)))(src));

const showToken = (t: Token) => (() => {
  if (t?._tag === `KwLet`) { return `KwLet`; }
  if (t?._tag === `KwTag`) { return `KwTag`; }
  if (t?._tag === `KwUnit`) { return `KwUnit`; }
  if (t?._tag === `KwTrait`) { return `KwTrait`; }
  if (t?._tag === `KwImpl`) { return `KwImpl`; }
  if (t?._tag === `KwImport`) { return `KwImport`; }
  if (t?._tag === `KwExport`) { return `KwExport`; }
  if (t?._tag === `KwModule`) { return `KwModule`; }
  if (t?._tag === `KwIf`) { return `KwIf`; }
  if (t?._tag === `KwElse`) { return `KwElse`; }
  if (t?._tag === `KwTrue`) { return `KwTrue`; }
  if (t?._tag === `KwFalse`) { return `KwFalse`; }
  if (t?._tag === `KwMatch`) { return `KwMatch`; }
  if (t?._tag === `Ident`) { const s = (t as any)._0; return ((((a: string) => (b: string): string => a + b))(`Ident:`))(s); }
  if (t?._tag === `TypeId`) { const s = (t as any)._0; return ((((a: string) => (b: string): string => a + b))(`TypeId:`))(s); }
  if (t?._tag === `IntLit`) { const n = (t as any)._0; return ((((a: string) => (b: string): string => a + b))(`Int:`))((((n: number): string => String(n)))(n)); }
  if (t?._tag === `FloatLit`) { const s = (t as any)._0; return ((((a: string) => (b: string): string => a + b))(`Float:`))(s); }
  if (t?._tag === `StrLit`) { const s = (t as any)._0; return ((((a: string) => (b: string): string => a + b))(`Str:`))(s); }
  if (t?._tag === `CharLit`) { const c = (t as any)._0; return ((((a: string) => (b: string): string => a + b))(`Char:`))((((cs: string[]): string => cs.join('')))([c])); }
  if (t?._tag === `Arrow`) { return `Arrow`; }
  if (t?._tag === `Backslash`) { return `Backslash`; }
  if (t?._tag === `Dot`) { return `Dot`; }
  if (t?._tag === `Comma`) { return `Comma`; }
  if (t?._tag === `Colon`) { return `Colon`; }
  if (t?._tag === `ColonColon`) { return `ColonColon`; }
  if (t?._tag === `Eq`) { return `Eq`; }
  if (t?._tag === `Bar`) { return `Bar`; }
  if (t?._tag === `LBrack`) { return `LBrack`; }
  if (t?._tag === `RBrack`) { return `RBrack`; }
  if (t?._tag === `LParen`) { return `LParen`; }
  if (t?._tag === `RParen`) { return `RParen`; }
  if (t?._tag === `Plus`) { return `Plus`; }
  if (t?._tag === `Minus`) { return `Minus`; }
  if (t?._tag === `Star`) { return `Star`; }
  if (t?._tag === `Slash`) { return `Slash`; }
  if (t?._tag === `Caret`) { return `Caret`; }
  if (t?._tag === `Lt`) { return `Lt`; }
  if (t?._tag === `Gt`) { return `Gt`; }
  if (t?._tag === `Le`) { return `Le`; }
  if (t?._tag === `Ge`) { return `Ge`; }
  if (t?._tag === `EqEq`) { return `EqEq`; }
  if (t?._tag === `Neq`) { return `Neq`; }
  if (t?._tag === `Underscore`) { return `Underscore`; }
  if (t?._tag === `Newline`) { return `Newline`; }
  if (t?._tag === `Indent`) { return `Indent`; }
  if (t?._tag === `Dedent`) { return `Dedent`; }
  if (t?._tag === `Eof`) { return `Eof`; }
  if (t?._tag === `TError`) { const c = (t as any)._0; return ((((a: string) => (b: string): string => a + b))(`Error:`))((((cs: string[]): string => cs.join('')))([c])); }
  throw new Error(`Non-exhaustive match`);
})();

const showTokens = (ts: Token[]) => (((((f: (acc: any) => (x: any) => any) => (z: any) => (xs: any[]): any => xs.reduce((a: any, x: any) => f(a)(x), z)))((acc: string) => (t: Token) => ((s) => (((((s: string): number => s.length))(acc) === 0) ? s : ((((a: string) => (b: string): string => a + b))(((((a: string) => (b: string): string => a + b))(acc))(` `)))(s)))((showToken)(t))))(``))(ts);

const stripNewlines = (ts: Token[]) => (() => {
  if (ts.length === 0) { return []; }
  return (stripNewlines)(rest);
  if (ts.length > 0) { const t = ts[0], rest = ts.slice(1); return [t, ...(stripNewlines)(rest)]; }
  throw new Error(`Non-exhaustive match`);
})();

const checkTokens = (label: string) => (src: string) => (expected: string) => ((got) => ((got === expected) ? (((s: string): void => { console.log(s) }))(((((a: string) => (b: string): string => a + b))(`OK `))(label)) : ((p1) => ((p2) => ((p3) => ((p4) => ((p5) => (((s: string): void => { console.log(s) }))(p5))(((((a: string) => (b: string): string => a + b))(p4))(got)))(((((a: string) => (b: string): string => a + b))(p3))(`\n  got:      `)))(((((a: string) => (b: string): string => a + b))(p2))(expected)))(((((a: string) => (b: string): string => a + b))(p1))(`\n  expected: `)))(((((a: string) => (b: string): string => a + b))(`FAIL `))(label))))((showTokens)((stripNewlines)((tokenize)(src))));

const __ll_main_Std_Lexer = () => ((_) => ((_) => ((_) => ((_) => ((_) => ((_) => ((_) => ((_) => ((_) => ((_) => ((_) => ((_) => 0)((((checkTokens)(`12 cons pattern`))(`x :: xs`))(`Ident:x ColonColon Ident:xs Eof`)))((((checkTokens)(`11 comparison ops`))(`!= <= >=`))(`Neq Le Ge Eof`)))((((checkTokens)(`10 all keywords`))(`let tag unit trait impl import export module if else true false match`))(`KwLet KwTag KwUnit KwTrait KwImpl KwImport KwExport KwModule KwIf KwElse KwTrue KwFalse KwMatch Eof`)))((((checkTokens)(`9 char literal`))(`'a'`))(`Char:a Eof`)))((((checkTokens)(`8 float`))(`3.14`))(`Float:3.14 Eof`)))((((checkTokens)(`7 comment`))(`x -- comment`))(`Ident:x Eof`)))((((checkTokens)(`6 mixed match`))(`match x | Some v -> v`))(`KwMatch Ident:x Bar TypeId:Some Ident:v Arrow Ident:v Eof`)))((((checkTokens)(`5 operators`))(`== -> |`))(`EqEq Arrow Bar Eof`)))((((checkTokens)(`4 string`))(`"hello"`))(`Str:hello Eof`)))((((checkTokens)(`3 integer`))(`42`))(`Int:42 Eof`)))((((checkTokens)(`2 identifiers`))(`foo Bar`))(`Ident:foo TypeId:Bar Eof`)))((((checkTokens)(`1 keywords`))(`match if else`))(`KwMatch KwIf KwElse Eof`));

type Pattern = { _tag: `PVar`; _0: string } | { _tag: `PWild` } | { _tag: `PCon`; _0: string; _1: Pattern[] } | { _tag: `PLitInt`; _0: number } | { _tag: `PLitStr`; _0: string } | { _tag: `PCons`; _0: Pattern; _1: Pattern } | { _tag: `PNil` };
const PVar = (_0: string): Pattern => ({ _tag: `PVar` as const, _0 });
const PWild: Pattern = { _tag: `PWild` as const };
const PCon = (_0: string, _1: Pattern[]): Pattern => ({ _tag: `PCon` as const, _0, _1 });
const PLitInt = (_0: number): Pattern => ({ _tag: `PLitInt` as const, _0 });
const PLitStr = (_0: string): Pattern => ({ _tag: `PLitStr` as const, _0 });
const PCons = (_0: Pattern, _1: Pattern): Pattern => ({ _tag: `PCons` as const, _0, _1 });
const PNil: Pattern = { _tag: `PNil` as const };

type Expr = { _tag: `EInt`; _0: number } | { _tag: `EStr`; _0: string } | { _tag: `EBool`; _0: boolean } | { _tag: `EChar`; _0: string } | { _tag: `EFloat`; _0: string } | { _tag: `EVar`; _0: string } | { _tag: `ECon`; _0: string } | { _tag: `EApp`; _0: Expr; _1: Expr } | { _tag: `EIf`; _0: Expr; _1: Expr; _2: Expr } | { _tag: `EMatch`; _0: Expr; _1: Pattern[]; _2: Expr[] } | { _tag: `ELam`; _0: string; _1: Expr } | { _tag: `ELet`; _0: string; _1: Expr; _2: Expr } | { _tag: `EList`; _0: Expr[] } | { _tag: `EBinOp`; _0: string; _1: Expr; _2: Expr } | { _tag: `ETuple`; _0: Expr; _1: Expr } | { _tag: `ENil` } | { _tag: `ECons`; _0: Expr; _1: Expr };
const EInt = (_0: number): Expr => ({ _tag: `EInt` as const, _0 });
const EStr = (_0: string): Expr => ({ _tag: `EStr` as const, _0 });
const EBool = (_0: boolean): Expr => ({ _tag: `EBool` as const, _0 });
const EChar = (_0: string): Expr => ({ _tag: `EChar` as const, _0 });
const EFloat = (_0: string): Expr => ({ _tag: `EFloat` as const, _0 });
const EVar = (_0: string): Expr => ({ _tag: `EVar` as const, _0 });
const ECon = (_0: string): Expr => ({ _tag: `ECon` as const, _0 });
const EApp = (_0: Expr, _1: Expr): Expr => ({ _tag: `EApp` as const, _0, _1 });
const EIf = (_0: Expr, _1: Expr, _2: Expr): Expr => ({ _tag: `EIf` as const, _0, _1, _2 });
const EMatch = (_0: Expr, _1: Pattern[], _2: Expr[]): Expr => ({ _tag: `EMatch` as const, _0, _1, _2 });
const ELam = (_0: string, _1: Expr): Expr => ({ _tag: `ELam` as const, _0, _1 });
const ELet = (_0: string, _1: Expr, _2: Expr): Expr => ({ _tag: `ELet` as const, _0, _1, _2 });
const EList = (_0: Expr[]): Expr => ({ _tag: `EList` as const, _0 });
const EBinOp = (_0: string, _1: Expr, _2: Expr): Expr => ({ _tag: `EBinOp` as const, _0, _1, _2 });
const ETuple = (_0: Expr, _1: Expr): Expr => ({ _tag: `ETuple` as const, _0, _1 });
const ENil: Expr = { _tag: `ENil` as const };
const ECons = (_0: Expr, _1: Expr): Expr => ({ _tag: `ECons` as const, _0, _1 });

type TypeExpr = { _tag: `TyName`; _0: string } | { _tag: `TyApp`; _0: TypeExpr; _1: TypeExpr } | { _tag: `TyFn`; _0: TypeExpr; _1: TypeExpr };
const TyName = (_0: string): TypeExpr => ({ _tag: `TyName` as const, _0 });
const TyApp = (_0: TypeExpr, _1: TypeExpr): TypeExpr => ({ _tag: `TyApp` as const, _0, _1 });
const TyFn = (_0: TypeExpr, _1: TypeExpr): TypeExpr => ({ _tag: `TyFn` as const, _0, _1 });

type Param = { _tag: `MkParam`; _0: string; _1: TypeExpr };
const MkParam = (_0: string, _1: TypeExpr): Param => ({ _tag: `MkParam` as const, _0, _1 });

type Constructor = { _tag: `MkCon`; _0: string; _1: TypeExpr[] };
const MkCon = (_0: string, _1: TypeExpr[]): Constructor => ({ _tag: `MkCon` as const, _0, _1 });

type Decl = { _tag: `DFn`; _0: string; _1: Param[]; _2: TypeExpr | null; _3: Expr } | { _tag: `DType`; _0: string; _1: string[]; _2: Constructor[] } | { _tag: `DImport`; _0: string[] } | { _tag: `DExport`; _0: Decl } | { _tag: `DLet`; _0: string; _1: Expr };
const DFn = (_0: string, _1: Param[], _2: TypeExpr | null, _3: Expr): Decl => ({ _tag: `DFn` as const, _0, _1, _2, _3 });
const DType = (_0: string, _1: string[], _2: Constructor[]): Decl => ({ _tag: `DType` as const, _0, _1, _2 });
const DImport = (_0: string[]): Decl => ({ _tag: `DImport` as const, _0 });
const DExport = (_0: Decl): Decl => ({ _tag: `DExport` as const, _0 });
const DLet = (_0: string, _1: Expr): Decl => ({ _tag: `DLet` as const, _0, _1 });

type Module = { _tag: `MkModule`; _0: string[]; _1: Decl[] };
const MkModule = (_0: string[], _1: Decl[]): Module => ({ _tag: `MkModule` as const, _0, _1 });

const skipNewlines = (toks: Token[]) => (() => {
  return (skipNewlines)(rest);
  return (skipNewlines)(rest);
  return (skipNewlines)(rest);
  return toks;
  throw new Error(`Non-exhaustive match`);
})();

const isAtomStart = (t: Token) => (() => {
  if (t?._tag === `IntLit`) {  return true; }
  if (t?._tag === `FloatLit`) {  return true; }
  if (t?._tag === `StrLit`) {  return true; }
  if (t?._tag === `CharLit`) {  return true; }
  if (t?._tag === `KwTrue`) { return true; }
  if (t?._tag === `KwFalse`) { return true; }
  if (t?._tag === `Ident`) {  return true; }
  if (t?._tag === `TypeId`) {  return true; }
  if (t?._tag === `LParen`) { return true; }
  if (t?._tag === `LBrack`) { return true; }
  return false;
  throw new Error(`Non-exhaustive match`);
})();

const skipBrackTypeArgs = (toks: Token[]) => (() => {
  return ((rest2) => (skipBrackTypeArgs)(rest2))((skipBrackTypeBody)(rest));
  return toks;
  throw new Error(`Non-exhaustive match`);
})();

const skipBrackTypeBody = (toks: Token[]) => (() => {
  return rest;
  return ((rest2) => (skipBrackTypeBody)(rest2))((skipBrackTypeBody)(rest));
  return (skipBrackTypeBody)(rest);
  if (toks.length === 0) { return []; }
  throw new Error(`Non-exhaustive match`);
})();

const parseTypeExpr = (toks: Token[]) => (() => {
  return ((rest2) => [TyName(name), rest2] as const)((skipBrackTypeArgs)(rest));
  return [TyName(name), rest] as const;
  return [TyName(`?`), toks] as const;
  throw new Error(`Non-exhaustive match`);
})();

const parseReturnType = (toks: Token[]) => (() => {
  return ((rest2) => [Some(TyName(name)), rest2] as const)((skipBrackTypeArgs)(rest));
  return [None, toks] as const;
  throw new Error(`Non-exhaustive match`);
})();

const parseParamGroups = (toks: Token[]) => (() => {
  return [[], rest] as const;
  return ((rest2) => ((rest3) => (([ps, rest4]) => [[MkParam(pname, TyName(tname)), ...ps], rest4] as const)((parseParamGroups)(rest3)))((() => {
  return r;
  return rest2;
  throw new Error(`Non-exhaustive match`);
})()))((skipBrackTypeArgs)(rest));
  return ((rest2) => ((rest3) => (([ps, rest4]) => [[MkParam(((((a: string) => (b: string): string => a + b))(`_`))(pname), TyName(tname)), ...ps], rest4] as const)((parseParamGroups)(rest3)))((() => {
  return r;
  return rest2;
  throw new Error(`Non-exhaustive match`);
})()))((skipBrackTypeArgs)(rest));
  return (([ps, rest2]) => [[MkParam(pname, TyName(`?`)), ...ps], rest2] as const)((parseParamGroups)(rest));
  return [[], toks] as const;
  throw new Error(`Non-exhaustive match`);
})();

const parseTypeParams = (toks: Token[]) => (() => {
  return (((((s: string): number => s.length))(s) === 1) ? (([ps, rest2]) => [[s, ...ps], rest2] as const)((parseTypeParams)(rest)) : [[], toks] as const);
  return [[], toks] as const;
  throw new Error(`Non-exhaustive match`);
})();

const parseConArgs = (toks: Token[]) => (() => {
  return (([arg, rest]) => (([args, rest2]) => [[arg, ...args], rest2] as const)((parseConArgs)(rest)))((parseTypeExpr)(toks));
  return [[], toks] as const;
  throw new Error(`Non-exhaustive match`);
})();

const parseCon = (toks: Token[]) => (() => {
  return (([args, rest2]) => [MkCon(name, args), rest2] as const)((parseConArgs)(rest));
  return [MkCon(`?`, []), toks] as const;
  throw new Error(`Non-exhaustive match`);
})();

const parseConsTail = (acc: Constructor[]) => (toks: Token[]) => ((toks2) => (() => {
  return ((rest2) => (([c, rest3]) => ((parseConsTail)(((((xs: any[]) => (ys: any[]): any[] => [...xs, ...ys]))(acc))([c])))(rest3))((parseCon)(rest2)))((skipNewlines)(rest));
  return [acc, toks] as const;
  throw new Error(`Non-exhaustive match`);
})())((skipNewlines)(toks));

const parseConList = (toks: Token[]) => ((toks2) => ((toks3) => (([c, rest]) => ((parseConsTail)([c]))(rest))((parseCon)(toks3)))((() => {
  return (skipNewlines)(r);
  return toks2;
  throw new Error(`Non-exhaustive match`);
})()))((skipNewlines)(toks));

const parseTypeDecl = (toks: Token[]) => (() => {
  return (([prms, rest2]) => ((rest3) => (([ctors, rest4]) => [DType(name, prms, ctors), rest4] as const)((parseConList)(rest3)))((() => {
  return (skipNewlines)(r);
  return (skipNewlines)(rest2);
  throw new Error(`Non-exhaustive match`);
})()))((parseTypeParams)(rest));
  return [DType(`?`, [], []), toks] as const;
  throw new Error(`Non-exhaustive match`);
})();

const parsePrimaryPat = (toks: Token[]) => (() => {
  return [PLitInt(n), rest] as const;
  return [PLitStr(s), rest] as const;
  return [PWild, rest] as const;
  return [PNil, rest] as const;
  return [PVar(s), rest] as const;
  return (([args, rest2]) => [PCon(name, args), rest2] as const)((parsePatArgs)(rest));
  return [PWild, toks] as const;
  throw new Error(`Non-exhaustive match`);
})();

const parsePatArgs = (toks: Token[]) => (() => {
  return (parsePatArgsCons)(toks);
  return (parsePatArgsCons)(toks);
  return (parsePatArgsCons)(toks);
  return (parsePatArgsCons)(toks);
  return [[], toks] as const;
  throw new Error(`Non-exhaustive match`);
})();

const parsePatArgsCons = (toks: Token[]) => (([p, rest]) => (([ps, rest2]) => [((((xs: any[]) => (ys: any[]): any[] => [...xs, ...ys]))([p]))(ps), rest2] as const)((parsePatArgs)(rest)))((parsePrimaryPat)(toks));

const parsePat = (toks: Token[]) => (([p, rest]) => (() => {
  return (([tail, rest3]) => [PCons(p, tail), rest3] as const)((parsePat)(rest2));
  return [p, rest] as const;
  throw new Error(`Non-exhaustive match`);
})())((parsePrimaryPat)(toks));

const parseArmBody = (toks: Token[]) => ((toks2) => (() => {
  return (parseIf)(rest);
  return (parseLetIn)(rest);
  return (parseLetIn)(toks2);
  return (parseCompare)(toks2);
  throw new Error(`Non-exhaustive match`);
})())((skipNewlines)(toks));

const skipArrow = (toks: Token[]) => (() => {
  return r;
  return toks;
  throw new Error(`Non-exhaustive match`);
})();

const parseArm = (toks: Token[]) => (() => {
  return (([p, rest2]) => ((rest3) => (([body, rest4]) => [[p, body] as const, rest4] as const)((parseArmBody)(rest3)))((skipArrow)(rest2)))((parsePat)(rest));
  return [[PWild, EInt(0)] as const, toks] as const;
  throw new Error(`Non-exhaustive match`);
})();

const parseArms = (toks: Token[]) => ((toks2) => (() => {
  return (([pb, rest]) => (([p, b]) => (([morePB, rest2]) => (([ps, bs]) => [[[p, ...ps], [b, ...bs]] as const, rest2] as const)(morePB))((parseArms)(rest)))(pb))((parseArm)(toks2));
  return [[[], []] as const, toks2] as const;
  throw new Error(`Non-exhaustive match`);
})())((skipNewlines)(toks));

const parseMatch = (toks: Token[]) => (([scrut, rest]) => (([armLists, rest2]) => (([pats, bodies]) => [EMatch(scrut, pats, bodies), rest2] as const)(armLists))((parseArms)(rest)))((parseExpr)(toks));

const parseIf = (toks: Token[]) => (([cond, rest]) => ((rest2) => (([thenE, rest3]) => ((rest3a) => ((rest4) => (([elseE, rest5]) => [EIf(cond, thenE, elseE), rest5] as const)((parseExpr)(rest4)))((() => {
  return (skipNewlines)(r);
  return rest3a;
  throw new Error(`Non-exhaustive match`);
})()))((skipNewlines)(rest3)))((parseExpr)(rest2)))((skipNewlines)(rest)))((parseExpr)(toks));

const skipKwIn = (toks: Token[]) => (() => {
  return (skipNewlines)(r);
  return (skipNewlines)(toks);
  throw new Error(`Non-exhaustive match`);
})();

const parseLetIn = (toks: Token[]) => (() => {
  return ((rest0) => (([e1, rest2]) => ((rest3) => (([e2, rest4]) => [ELet(a, e1, ELet(b, ETuple(e1, e2), e2)), rest4] as const)((parseExpr)(rest3)))((skipKwIn)(rest2)))((parseExpr)(rest0)))((skipNewlines)(rest));
  return ((rest0) => (([e1, rest2]) => ((rest3) => (([e2, rest4]) => [ELet(name, e1, e2), rest4] as const)((parseExpr)(rest3)))((skipKwIn)(rest2)))((parseExpr)(rest0)))((skipNewlines)(rest));
  return [EInt(0), toks] as const;
  throw new Error(`Non-exhaustive match`);
})();

const parseLamParams = (acc: string[]) => (toks: Token[]) => (() => {
  return ((parseLamParams)(((((xs: any[]) => (ys: any[]): any[] => [...xs, ...ys]))(acc))([name])))(rest);
  return [acc, toks] as const;
  throw new Error(`Non-exhaustive match`);
})();

const wrapLamParams = (parms: string[]) => (body: Expr) => (() => {
  if (parms.length > 0) { const p = parms[0], rest = parms.slice(1); return ELam(p, ((wrapLamParams)(rest))(body)); }
  return body;
  throw new Error(`Non-exhaustive match`);
})();

const parseLam = (toks: Token[]) => (([parms, rest]) => (() => {
  return (([body, rest3]) => [((wrapLamParams)(parms))(body), rest3] as const)((parseExpr)(rest2));
  return [EInt(0), toks] as const;
  throw new Error(`Non-exhaustive match`);
})())(((parseLamParams)([]))(toks));

const parseExpr = (toks: Token[]) => (() => {
  return (parseIf)(rest);
  return (parseMatch)(rest);
  return (parseLetIn)(rest);
  return (parseLetIn)(toks);
  return (parseLam)(rest);
  return (parseCompare)(toks);
  throw new Error(`Non-exhaustive match`);
})();

const parseCompare = (toks: Token[]) => (([e, rest]) => ((parseCompareTail)(e))(rest))((parseCons)(toks));

const parseCompareTail = (lhs: Expr) => (toks: Token[]) => (() => {
  return (([rhs, rest2]) => ((parseCompareTail)(EBinOp(`==`, lhs, rhs)))(rest2))((parseCons)(r));
  return (([rhs, rest2]) => ((parseCompareTail)(EBinOp(`!=`, lhs, rhs)))(rest2))((parseCons)(r));
  return (([rhs, rest2]) => ((parseCompareTail)(EBinOp(`<`, lhs, rhs)))(rest2))((parseCons)(r));
  return (([rhs, rest2]) => ((parseCompareTail)(EBinOp(`>`, lhs, rhs)))(rest2))((parseCons)(r));
  return (([rhs, rest2]) => ((parseCompareTail)(EBinOp(`<=`, lhs, rhs)))(rest2))((parseCons)(r));
  return (([rhs, rest2]) => ((parseCompareTail)(EBinOp(`>=`, lhs, rhs)))(rest2))((parseCons)(r));
  return [lhs, toks] as const;
  throw new Error(`Non-exhaustive match`);
})();

const parseCons = (toks: Token[]) => (([head, rest]) => (() => {
  return (([tail, rest3]) => [ECons(head, tail), rest3] as const)((parseCons)(rest2));
  return [head, rest] as const;
  throw new Error(`Non-exhaustive match`);
})())((parseAddSub)(toks));

const parseAddSub = (toks: Token[]) => (([e, rest]) => ((parseAddSubTail)(e))(rest))((parseMulDiv)(toks));

const parseAddSubTail = (lhs: Expr) => (toks: Token[]) => (() => {
  return (([r, rest2]) => ((parseAddSubTail)(EBinOp(`+`, lhs, r)))(rest2))((parseMulDiv)(rest));
  return (([r, rest2]) => ((parseAddSubTail)(EBinOp(`-`, lhs, r)))(rest2))((parseMulDiv)(rest));
  return [lhs, toks] as const;
  throw new Error(`Non-exhaustive match`);
})();

const parseMulDiv = (toks: Token[]) => (([e, rest]) => ((parseMulDivTail)(e))(rest))((parseApp)(toks));

const parseMulDivTail = (lhs: Expr) => (toks: Token[]) => (() => {
  return (([r, rest2]) => ((parseMulDivTail)(EBinOp(`*`, lhs, r)))(rest2))((parseApp)(rest));
  return (([r, rest2]) => ((parseMulDivTail)(EBinOp(`/`, lhs, r)))(rest2))((parseApp)(rest));
  return [lhs, toks] as const;
  throw new Error(`Non-exhaustive match`);
})();

const parseApp = (toks: Token[]) => (([e, rest]) => ((parseAppTail)(e))(rest))((parseAtom)(toks));

const parseAppTail = (lhs: Expr) => (toks: Token[]) => (() => {
  return ((isAtomStart)(t) ? (([arg, rest]) => ((parseAppTail)(EApp(lhs, arg)))(rest))((parseAtom)(toks)) : [lhs, toks] as const);
  return [lhs, toks] as const;
  throw new Error(`Non-exhaustive match`);
})();

const parseListLit = (toks: Token[]) => (() => {
  return [ENil, rest] as const;
  return (([e, rest]) => (() => {
  return [ECons(e, ENil), rest2] as const;
  return (([tail, rest3]) => [ECons(e, tail), rest3] as const)((parseListLit)(rest2));
  return (([tail, rest2]) => [ECons(e, tail), rest2] as const)((parseListLit)(rest));
  throw new Error(`Non-exhaustive match`);
})())((parseListElem)(toks));
  throw new Error(`Non-exhaustive match`);
})();

const parseListElem = (toks: Token[]) => (() => {
  return (parseApp)(toks);
  return (parseAtom)(toks);
  throw new Error(`Non-exhaustive match`);
})();

const parseAtomParenTail = (e: Expr) => (toks: Token[]) => (() => {
  return ((rest2) => (([e2, rest3]) => (() => {
  return [ETuple(e, e2), rest4] as const;
  return [ETuple(e, e2), rest3] as const;
  throw new Error(`Non-exhaustive match`);
})())((parseExpr)(rest2)))((skipNewlines)(rest));
  return [e, rest] as const;
  return [e, toks] as const;
  throw new Error(`Non-exhaustive match`);
})();

const parseAtom = (toks: Token[]) => (() => {
  return [EInt(n), rest] as const;
  return [EFloat(s), rest] as const;
  return [EStr(s), rest] as const;
  return [EChar(c), rest] as const;
  return [EBool(true), rest] as const;
  return [EBool(false), rest] as const;
  return [EVar(s), rest] as const;
  return [ECon(s), rest] as const;
  return [ENil, rest] as const;
  return (parseListLit)(rest);
  return (([e, rest2]) => ((parseAtomParenTail)(e))(rest2))((parseExpr)(rest));
  return [EInt(0), toks] as const;
  throw new Error(`Non-exhaustive match`);
})();

const lastParamVar = (prms: Param[]) => (() => {
  if (prms.length === 0) { return EInt(0); }
  return EVar(n);
  return (lastParamVar)(rest);
  throw new Error(`Non-exhaustive match`);
})();

const parseFnBody = (prms: Param[]) => (toks: Token[]) => ((toks2) => (() => {
  return (([armLists, rest]) => (([pats, bodies]) => ((scrut) => [EMatch(scrut, pats, bodies), rest] as const)((lastParamVar)(prms)))(armLists))((parseArms)(toks2));
  return (parseExpr)(toks2);
  throw new Error(`Non-exhaustive match`);
})())((skipNewlines)(toks));

const parseFnDecl = (toks: Token[]) => (() => {
  return (([prms, rest2]) => (([retTy, rest3]) => ((rest4) => (([body, rest5]) => [DFn(name, prms, retTy, body), rest5] as const)(((parseFnBody)(prms))(rest4)))((() => {
  return (skipNewlines)(r);
  return (skipNewlines)(rest3);
  throw new Error(`Non-exhaustive match`);
})()))((parseReturnType)(rest2)))((parseParamGroups)(rest));
  return [DFn(`?`, [], None, EInt(0)), toks] as const;
  throw new Error(`Non-exhaustive match`);
})();

const parseLetDecl = (toks: Token[]) => (() => {
  return ((rest0) => (([body, rest2]) => [DLet(name, body), rest2] as const)((parseExpr)(rest0)))((skipNewlines)(rest));
  return [DLet(`?`, EInt(0)), toks] as const;
  throw new Error(`Non-exhaustive match`);
})();

const parseImportPath = (acc: string[]) => (toks: Token[]) => (() => {
  return ((parseImportPath)(((((xs: any[]) => (ys: any[]): any[] => [...xs, ...ys]))(acc))([seg])))(rest);
  return [acc, toks] as const;
  throw new Error(`Non-exhaustive match`);
})();

const parseImportDecl = (toks: Token[]) => (() => {
  return (([segs, rest2]) => [DImport(segs), rest2] as const)(((parseImportPath)([head]))(rest));
  return [DImport([]), toks] as const;
  throw new Error(`Non-exhaustive match`);
})();

const parseOneDecl = (toks: Token[]) => (() => {
  return (parseTypeDecl)(toks);
  return (parseLetDecl)(toks);
  return (parseFnDecl)(toks);
  return (parseImportDecl)(toks);
  return (([inner, rest2]) => [DExport(inner), rest2] as const)((parseOneDecl)(rest));
  return [DLet(`?`, EInt(0)), toks] as const;
  throw new Error(`Non-exhaustive match`);
})();

const parseDecls = (toks: Token[]) => ((toks2) => (() => {
  return [];
  if (toks2.length === 0) { return []; }
  return (([d, rest]) => [d, ...(parseDecls)(rest)])((parseTypeDecl)(toks2));
  return (([d, rest]) => [d, ...(parseDecls)(rest)])((parseLetDecl)(toks2));
  return (([d, rest]) => [d, ...(parseDecls)(rest)])((parseFnDecl)(toks2));
  return (([d, rest]) => [d, ...(parseDecls)(rest)])((parseImportDecl)(toks2));
  return (([inner, rest2]) => [DExport(inner), ...(parseDecls)(rest2)])((parseOneDecl)(rest));
  return [];
  throw new Error(`Non-exhaustive match`);
})())((skipNewlines)(toks));

const parseModulePath = (acc: string[]) => (toks: Token[]) => (() => {
  return ((parseModulePath)(((((xs: any[]) => (ys: any[]): any[] => [...xs, ...ys]))(acc))([seg])))(rest);
  return [acc, toks] as const;
  throw new Error(`Non-exhaustive match`);
})();

const parseModuleHeader = (toks: Token[]) => (() => {
  return (([segs, rest2]) => [segs, rest2] as const)(((parseModulePath)([head]))(rest));
  return [[], toks] as const;
  throw new Error(`Non-exhaustive match`);
})();

const parseModule = (toks: Token[]) => ((toks2) => (([path, rest]) => ((decls) => MkModule(path, decls))((parseDecls)((skipNewlines)(rest))))((parseModuleHeader)(toks2)))((skipNewlines)(toks));

const boolToStr = (b: boolean) => (b ? `true` : `false`);

const showExpr = (e: Expr) => (() => {
  if (e?._tag === `EInt`) { const n = (e as any)._0; return ((((a: string) => (b: string): string => a + b))(`EInt `))((((n: number): string => String(n)))(n)); }
  if (e?._tag === `EStr`) { const s = (e as any)._0; return ((((a: string) => (b: string): string => a + b))(`EStr `))(s); }
  if (e?._tag === `EBool`) { const b = (e as any)._0; return ((((a: string) => (b: string): string => a + b))(`EBool `))((boolToStr)(b)); }
  if (e?._tag === `EChar`) { const c = (e as any)._0; return ((((a: string) => (b: string): string => a + b))(`EChar `))((((cs: string[]): string => cs.join('')))([c])); }
  if (e?._tag === `EFloat`) { const s = (e as any)._0; return ((((a: string) => (b: string): string => a + b))(`EFloat `))(s); }
  if (e?._tag === `EVar`) { const s = (e as any)._0; return ((((a: string) => (b: string): string => a + b))(`EVar `))(s); }
  if (e?._tag === `ECon`) { const s = (e as any)._0; return ((((a: string) => (b: string): string => a + b))(`ECon `))(s); }
  if (e?._tag === `ENil`) { return `ENil`; }
  if (e?._tag === `ECons`) { const h = (e as any)._0; const t = (e as any)._1; return ((((a: string) => (b: string): string => a + b))(((((a: string) => (b: string): string => a + b))(`ECons(`))((showExpr)(h))))(((((a: string) => (b: string): string => a + b))(` `))(((((a: string) => (b: string): string => a + b))((showExpr)(t)))(`)`))); }
  if (e?._tag === `EApp`) { const f = (e as any)._0; const x = (e as any)._1; return ((((a: string) => (b: string): string => a + b))(((((a: string) => (b: string): string => a + b))(`EApp(`))((showExpr)(f))))(((((a: string) => (b: string): string => a + b))(` `))(((((a: string) => (b: string): string => a + b))((showExpr)(x)))(`)`))); }
  if (e?._tag === `EIf`) { const c = (e as any)._0; const a = (e as any)._1; const b = (e as any)._2; return ((((a: string) => (b: string): string => a + b))(`EIf(`))(((((a: string) => (b: string): string => a + b))((showExpr)(c)))(((((a: string) => (b: string): string => a + b))(` `))(((((a: string) => (b: string): string => a + b))((showExpr)(a)))(((((a: string) => (b: string): string => a + b))(` `))(((((a: string) => (b: string): string => a + b))((showExpr)(b)))(`)`)))))); }
  if (e?._tag === `EBinOp`) { const op = (e as any)._0; const l = (e as any)._1; const r = (e as any)._2; return ((((a: string) => (b: string): string => a + b))(`EBinOp(`))(((((a: string) => (b: string): string => a + b))(op))(((((a: string) => (b: string): string => a + b))(` `))(((((a: string) => (b: string): string => a + b))((showExpr)(l)))(((((a: string) => (b: string): string => a + b))(` `))(((((a: string) => (b: string): string => a + b))((showExpr)(r)))(`)`)))))); }
  if (e?._tag === `ETuple`) { const a = (e as any)._0; const b = (e as any)._1; return ((((a: string) => (b: string): string => a + b))(`ETuple(`))(((((a: string) => (b: string): string => a + b))((showExpr)(a)))(((((a: string) => (b: string): string => a + b))(` `))(((((a: string) => (b: string): string => a + b))((showExpr)(b)))(`)`)))); }
  if (e?._tag === `ELet`) { const n = (e as any)._0; const e1 = (e as any)._1; const e2 = (e as any)._2; return ((((a: string) => (b: string): string => a + b))(`ELet `))(((((a: string) => (b: string): string => a + b))(n))(((((a: string) => (b: string): string => a + b))(`=(`))(((((a: string) => (b: string): string => a + b))((showExpr)(e1)))(((((a: string) => (b: string): string => a + b))(`) in (`))(((((a: string) => (b: string): string => a + b))((showExpr)(e2)))(`)`)))))); }
  if (e?._tag === `ELam`) { const n = (e as any)._0; const b = (e as any)._1; return ((((a: string) => (b: string): string => a + b))(`ELam `))(((((a: string) => (b: string): string => a + b))(n))(((((a: string) => (b: string): string => a + b))(`.`))((showExpr)(b)))); }
  if (e?._tag === `EMatch`) { const s = (e as any)._0; const pats = (e as any)._1; const bodies = (e as any)._2; return ((((a: string) => (b: string): string => a + b))(`EMatch(`))(((((a: string) => (b: string): string => a + b))((showExpr)(s)))(`...)`)); }
  if (e?._tag === `EList`) { const es = (e as any)._0; return `EList(...)`; }
  throw new Error(`Non-exhaustive match`);
})();

const showPattern = (p: Pattern) => (() => {
  if (p?._tag === `PVar`) { const s = (p as any)._0; return ((((a: string) => (b: string): string => a + b))(`PVar `))(s); }
  if (p?._tag === `PWild`) { return `PWild`; }
  if (p?._tag === `PCon`) { const name = (p as any)._0; const args = (p as any)._1; return ((((a: string) => (b: string): string => a + b))(`PCon `))(name); }
  if (p?._tag === `PLitInt`) { const n = (p as any)._0; return ((((a: string) => (b: string): string => a + b))(`PLitInt `))((((n: number): string => String(n)))(n)); }
  if (p?._tag === `PLitStr`) { const s = (p as any)._0; return ((((a: string) => (b: string): string => a + b))(`PLitStr `))(s); }
  if (p?._tag === `PCons`) { const h = (p as any)._0; const t = (p as any)._1; return ((((a: string) => (b: string): string => a + b))(`PCons(`))(((((a: string) => (b: string): string => a + b))((showPattern)(h)))(((((a: string) => (b: string): string => a + b))(` `))(((((a: string) => (b: string): string => a + b))((showPattern)(t)))(`)`)))); }
  if (p?._tag === `PNil`) { return `PNil`; }
  throw new Error(`Non-exhaustive match`);
})();

const showMaybeTy = (m: TypeExpr | null) => (() => {
  if (m?._tag === `Some`) {  return `Some`; }
  if (m?._tag === `None`) { return `None`; }
  throw new Error(`Non-exhaustive match`);
})();

const joinDot = (segs: string[]) => (((((f: (acc: any) => (x: any) => any) => (z: any) => (xs: any[]): any => xs.reduce((a: any, x: any) => f(a)(x), z)))((acc: string) => (s: string) => (((((s: string): number => s.length))(acc) === 0) ? s : ((((a: string) => (b: string): string => a + b))(((((a: string) => (b: string): string => a + b))(acc))(`.`)))(s))))(``))(segs);

const showDecl = (d: Decl) => (() => {
  if (d?._tag === `DFn`) { const name = (d as any)._0; const prms = (d as any)._1; const retTy = (d as any)._2; const body = (d as any)._3; return ((((a: string) => (b: string): string => a + b))(`DFn `))(((((a: string) => (b: string): string => a + b))(name))(((((a: string) => (b: string): string => a + b))(` body=(`))(((((a: string) => (b: string): string => a + b))((showExpr)(body)))(`)`)))); }
  if (d?._tag === `DType`) { const name = (d as any)._0; const prms = (d as any)._1; const ctors = (d as any)._2; return ((((a: string) => (b: string): string => a + b))(`DType `))(name); }
  if (d?._tag === `DImport`) { const segs = (d as any)._0; return ((path) => ((((a: string) => (b: string): string => a + b))(`DImport `))(path))((joinDot)(segs)); }
  if (d?._tag === `DExport`) { const inner = (d as any)._0; return ((((a: string) => (b: string): string => a + b))(`DExport(`))(((((a: string) => (b: string): string => a + b))((showDecl)(inner)))(`)`)); }
  if (d?._tag === `DLet`) { const name = (d as any)._0; const body = (d as any)._1; return ((((a: string) => (b: string): string => a + b))(`DLet `))(((((a: string) => (b: string): string => a + b))(name))(((((a: string) => (b: string): string => a + b))(`=(`))(((((a: string) => (b: string): string => a + b))((showExpr)(body)))(`)`)))); }
  throw new Error(`Non-exhaustive match`);
})();

const parserCheckExpr = (label: string) => (toks: Token[]) => (expected: string) => (([e, _rest]) => ((got) => ((got === expected) ? (((s: string): void => { console.log(s) }))(((((a: string) => (b: string): string => a + b))(`OK `))(label)) : ((p1) => ((p2) => ((p3) => ((p4) => ((p5) => (((s: string): void => { console.log(s) }))(p5))(((((a: string) => (b: string): string => a + b))(p4))(got)))(((((a: string) => (b: string): string => a + b))(p3))(`\n  got:      `)))(((((a: string) => (b: string): string => a + b))(p2))(expected)))(((((a: string) => (b: string): string => a + b))(p1))(`\n  expected: `)))(((((a: string) => (b: string): string => a + b))(`FAIL `))(label))))((showExpr)(e)))((parseExpr)((skipNewlines)(toks)));

const parserCheckDecl = (label: string) => (toks: Token[]) => (expected: string) => (([d, _rest]) => ((got) => ((got === expected) ? (((s: string): void => { console.log(s) }))(((((a: string) => (b: string): string => a + b))(`OK `))(label)) : ((p1) => ((p2) => ((p3) => ((p4) => ((p5) => (((s: string): void => { console.log(s) }))(p5))(((((a: string) => (b: string): string => a + b))(p4))(got)))(((((a: string) => (b: string): string => a + b))(p3))(`\n  got:      `)))(((((a: string) => (b: string): string => a + b))(p2))(expected)))(((((a: string) => (b: string): string => a + b))(p1))(`\n  expected: `)))(((((a: string) => (b: string): string => a + b))(`FAIL `))(label))))((showDecl)(d)))((parseOneDecl)((skipNewlines)(toks)));

const parserCheckModule = (label: string) => (toks: Token[]) => (expectedDeclCount: number) => ((m) => (() => {
  if (m?._tag === `MkModule`) { const path = (m as any)._0; const decls = (m as any)._1; return ((n) => ((n === expectedDeclCount) ? (((s: string): void => { console.log(s) }))(((((a: string) => (b: string): string => a + b))(`OK `))(label)) : ((p1) => ((p2) => ((p3) => ((p4) => ((p5) => (((s: string): void => { console.log(s) }))(p5))(((((a: string) => (b: string): string => a + b))(p4))((((n: number): string => String(n)))(n))))(((((a: string) => (b: string): string => a + b))(p3))(` decls, got `)))(((((a: string) => (b: string): string => a + b))(p2))((((n: number): string => String(n)))(expectedDeclCount))))(((((a: string) => (b: string): string => a + b))(p1))(` expected `)))(((((a: string) => (b: string): string => a + b))(`FAIL `))(label))))((((xs: any[]): number => xs.length))(decls)); }
  throw new Error(`Non-exhaustive match`);
})())((parseModule)(toks));

const __ll_main_Std_Parser = () => ((t1) => ((_) => ((t2) => ((_) => ((t3) => ((_) => ((t4) => ((_) => ((t5) => ((_) => ((t6) => ((_) => ((t7) => ((_) => ((t8) => ((_) => ((t9) => ((_) => ((t10) => ((_) => ((t11) => ((_) => ((t12) => ((_) => ((t13) => ((_) => ((t14) => ((_) => ((t15) => ((_) => ((t16) => ((_) => ((t17) => ((_) => ((t18) => ((_) => ((t19) => ((_) => ((t20) => ((_) => ((t21) => ((_) => ((t22) => ((_) => ((t23) => ((_) => ((t24) => ((_) => ((t25) => ((_) => 0)((((parserCheckExpr)(`25 tuple`))(t25))(`ETuple(EVar a EVar b)`)))([LParen, ...[Ident(`a`), ...[Comma, ...[Ident(`b`), ...[RParen, ...[Eof, ...[]]]]]]]))((((parserCheckExpr)(`24 paren grouping`))(t24))(`EVar x`)))([LParen, ...[Ident(`x`), ...[RParen, ...[Eof, ...[]]]]]))((((parserCheckExpr)(`23 precedence`))(t23))(`EBinOp(+ EInt 1 EBinOp(* EInt 2 EInt 3))`)))([IntLit(1), ...[Plus, ...[IntLit(2), ...[Star, ...[IntLit(3), ...[Eof, ...[]]]]]]]))((((parserCheckExpr)(`22 multiply`))(t22))(`EBinOp(* EInt 2 EInt 3)`)))([IntLit(2), ...[Star, ...[IntLit(3), ...[Eof, ...[]]]]]))((((parserCheckExpr)(`21 nested app`))(t21))(`EApp(EApp(EVar f EVar x) EVar y)`)))([Ident(`f`), ...[Ident(`x`), ...[Ident(`y`), ...[Eof, ...[]]]]]))((((parserCheckModule)(`20 module`))(t20))(2)))([KwModule, ...[TypeId(`Test`), ...[Newline, ...[TypeId(`Foo`), ...[Eq, ...[TypeId(`Bar`), ...[Newline, ...[Ident(`f`), ...[LParen, ...[Ident(`x`), ...[TypeId(`Int`), ...[RParen, ...[Eq, ...[Ident(`x`), ...[Eof, ...[]]]]]]]]]]]]]]]]))((((parserCheckDecl)(`19 let decl`))(t19))(`DLet x=(EInt 99)`)))([KwLet, ...[Ident(`x`), ...[Eq, ...[IntLit(99), ...[Eof, ...[]]]]]]))((((parserCheckDecl)(`18 import`))(t18))(`DImport Std.Lexer`)))([KwImport, ...[TypeId(`Std`), ...[Dot, ...[TypeId(`Lexer`), ...[Eof, ...[]]]]]]))((((parserCheckDecl)(`17 type decl`))(t17))(`DType Maybe`)))([TypeId(`Maybe`), ...[TypeId(`A`), ...[Eq, ...[TypeId(`Some`), ...[TypeId(`A`), ...[Bar, ...[TypeId(`None`), ...[Eof, ...[]]]]]]]]]))((((parserCheckDecl)(`16 fn decl`))(t16))(`DFn add body=(EBinOp(+ EVar a EVar b))`)))([Ident(`add`), ...[LParen, ...[Ident(`a`), ...[TypeId(`Int`), ...[RParen, ...[LParen, ...[Ident(`b`), ...[TypeId(`Int`), ...[RParen, ...[Eq, ...[Ident(`a`), ...[Plus, ...[Ident(`b`), ...[Eof, ...[]]]]]]]]]]]]]]]))((((parserCheckExpr)(`15 match`))(t15))(`EMatch(EVar x...)`)))([KwMatch, ...[Ident(`x`), ...[Newline, ...[Bar, ...[TypeId(`Some`), ...[Ident(`v`), ...[Arrow, ...[Ident(`v`), ...[Newline, ...[Bar, ...[TypeId(`None`), ...[Arrow, ...[IntLit(0), ...[Eof, ...[]]]]]]]]]]]]]]]))((((parserCheckExpr)(`14 cons`))(t14))(`ECons(EVar x EVar xs)`)))([Ident(`x`), ...[ColonColon, ...[Ident(`xs`), ...[Eof, ...[]]]]]))((((parserCheckExpr)(`13 empty list`))(t13))(`ENil`)))([LBrack, ...[RBrack, ...[Eof, ...[]]]]))((((parserCheckExpr)(`12 constructor app`))(t12))(`EApp(ECon Some EInt 42)`)))([TypeId(`Some`), ...[IntLit(42), ...[Eof, ...[]]]]))((((parserCheckExpr)(`11 lambda`))(t11))(`ELam x.EVar x`)))([Backslash, ...[Ident(`x`), ...[Dot, ...[Ident(`x`), ...[Eof, ...[]]]]]]))((((parserCheckExpr)(`10 let`))(t10))(`ELet x=(EInt 42) in (EVar x)`)))([Ident(`x`), ...[Eq, ...[IntLit(42), ...[Newline, ...[Ident(`x`), ...[Eof, ...[]]]]]]]))((((parserCheckExpr)(`9 if-else`))(t9))(`EIf(EBool true EInt 1 EInt 2)`)))([KwIf, ...[KwTrue, ...[Newline, ...[IntLit(1), ...[Newline, ...[KwElse, ...[IntLit(2), ...[Eof, ...[]]]]]]]]]))((((parserCheckExpr)(`8 comparison`))(t8))(`EBinOp(== EVar x EInt 42)`)))([Ident(`x`), ...[EqEq, ...[IntLit(42), ...[Eof, ...[]]]]]))((((parserCheckExpr)(`7 addition`))(t7))(`EBinOp(+ EInt 1 EInt 2)`)))([IntLit(1), ...[Plus, ...[IntLit(2), ...[Eof, ...[]]]]]))((((parserCheckExpr)(`6 bool false`))(t6))(`EBool false`)))([KwFalse, ...[Eof, ...[]]]))((((parserCheckExpr)(`5 bool true`))(t5))(`EBool true`)))([KwTrue, ...[Eof, ...[]]]))((((parserCheckExpr)(`4 string literal`))(t4))(`EStr hello`)))([StrLit(`hello`), ...[Eof, ...[]]]))((((parserCheckExpr)(`3 application`))(t3))(`EApp(EVar f EVar x)`)))([Ident(`f`), ...[Ident(`x`), ...[Eof, ...[]]]]))((((parserCheckExpr)(`2 variable`))(t2))(`EVar x`)))([Ident(`x`), ...[Eof, ...[]]]))((((parserCheckExpr)(`1 int literal`))(t1))(`EInt 42`)))([IntLit(42), ...[Eof, ...[]]]);

const joinWith = (sep: string) => (items: string[]) => (() => {
  if (items.length === 0) { return ``; }
  return x;
  if (items.length > 0) { const x = items[0], rest = items.slice(1); return ((((a: string) => (b: string): string => a + b))(x))(((((a: string) => (b: string): string => a + b))(sep))(((joinWith)(sep))(rest))); }
  throw new Error(`Non-exhaustive match`);
})();

const encodeStrEscape = (c: string) => (() => {
  if (c === `\n`) { return `\\n`; }
  if (c === `\t`) { return `\\t`; }
  if (c === `\r`) { return `\\r`; }
  if (c === `\\`) { return `\\\\`; }
  if (c === `"`) { return `\\"`; }
  return (((cs: string[]): string => cs.join('')))([c]);
  throw new Error(`Non-exhaustive match`);
})();

const escapeStr = (s: string) => (((((f: (acc: any) => (x: any) => any) => (z: any) => (xs: any[]): any => xs.reduce((a: any, x: any) => f(a)(x), z)))((acc: string) => (c: string) => ((((a: string) => (b: string): string => a + b))(acc))((encodeStrEscape)(c))))(``))((((s: string): string[] => Array.from(s)))(s));

const emitCharLit = (c: string) => (() => {
  if (c === `\n`) { return `'\\n'`; }
  if (c === `\t`) { return `'\\t'`; }
  if (c === `\\`) { return `'\\\\'`; }
  if (c === `'`) { return `'\\''`; }
  return ((((a: string) => (b: string): string => a + b))(((((a: string) => (b: string): string => a + b))(`'`))((((cs: string[]): string => cs.join('')))([c]))))(`'`);
  throw new Error(`Non-exhaustive match`);
})();

const emitType = (t: TypeExpr) => (() => {
  if (t?._tag === `TyName`) { const name = (t as any)._0; return name; }
  if (t?._tag === `TyApp`) { const head = (t as any)._0; const arg = (t as any)._1; return ((((a: string) => (b: string): string => a + b))((emitType)(head)))(((((a: string) => (b: string): string => a + b))(`[`))(((((a: string) => (b: string): string => a + b))((emitType)(arg)))(`]`))); }
  if (t?._tag === `TyFn`) { const a = (t as any)._0; const b = (t as any)._1; return ((((a: string) => (b: string): string => a + b))((emitTypeAtom)(a)))(((((a: string) => (b: string): string => a + b))(` -> `))((emitType)(b))); }
  throw new Error(`Non-exhaustive match`);
})();

const emitTypeAtom = (t: TypeExpr) => (() => {
  if (t?._tag === `TyFn`) {  return ((((a: string) => (b: string): string => a + b))(`(`))(((((a: string) => (b: string): string => a + b))((emitType)(t)))(`)`)); }
  return (emitType)(t);
  throw new Error(`Non-exhaustive match`);
})();

const emitParam = (p: Param) => (() => {
  if (p?._tag === `MkParam`) { const name = (p as any)._0; const ty = (p as any)._1; return ((((a: string) => (b: string): string => a + b))(`(`))(((((a: string) => (b: string): string => a + b))(name))(((((a: string) => (b: string): string => a + b))(` `))(((((a: string) => (b: string): string => a + b))((emitType)(ty)))(`)`)))); }
  throw new Error(`Non-exhaustive match`);
})();

const emitParams = (ps: Param[]) => ((joinWith)(``))(((((f: (x: any) => any) => (xs: any[]): any[] => xs.map(f)))(emitParam))(ps));

const emitCtor = (c: Constructor) => (() => {
  if (c?._tag === `MkCon`) { const name = (c as any)._0; const args = (c as any)._1; return (() => {
  if (args.length === 0) { return name; }
  return ((((a: string) => (b: string): string => a + b))(name))(((((a: string) => (b: string): string => a + b))(` `))(((joinWith)(` `))(((((f: (x: any) => any) => (xs: any[]): any[] => xs.map(f)))(emitTypeAtom))(args))));
  throw new Error(`Non-exhaustive match`);
})(); }
  throw new Error(`Non-exhaustive match`);
})();

const isAtomicExpr = (e: Expr) => (() => {
  if (e?._tag === `EInt`) {  return true; }
  if (e?._tag === `EStr`) {  return true; }
  if (e?._tag === `EBool`) {  return true; }
  if (e?._tag === `EChar`) {  return true; }
  if (e?._tag === `EFloat`) {  return true; }
  if (e?._tag === `EVar`) {  return true; }
  if (e?._tag === `ECon`) {  return true; }
  if (e?._tag === `ENil`) { return true; }
  return false;
  throw new Error(`Non-exhaustive match`);
})();

const emitExprAtom = (e: Expr) => ((isAtomicExpr)(e) ? (emitExpr)(e) : ((((a: string) => (b: string): string => a + b))(`(`))(((((a: string) => (b: string): string => a + b))((emitExpr)(e)))(`)`)));

const emitPattern = (p: Pattern) => (() => {
  if (p?._tag === `PVar`) { const name = (p as any)._0; return name; }
  if (p?._tag === `PWild`) { return `_`; }
  if (p?._tag === `PNil`) { return `[]`; }
  if (p?._tag === `PLitInt`) { const n = (p as any)._0; return (((n: number): string => String(n)))(n); }
  if (p?._tag === `PLitStr`) { const s = (p as any)._0; return ((((a: string) => (b: string): string => a + b))(`"`))(((((a: string) => (b: string): string => a + b))((escapeStr)(s)))(`"`)); }
  if (p?._tag === `PCons`) { const h = (p as any)._0; const t = (p as any)._1; return ((((a: string) => (b: string): string => a + b))((emitPatternAtom)(h)))(((((a: string) => (b: string): string => a + b))(` :: `))((emitPattern)(t))); }
  if (p?._tag === `PCon`) { const name = (p as any)._0; const args = (p as any)._1; return (() => {
  if (args.length === 0) { return name; }
  return ((((a: string) => (b: string): string => a + b))(name))(((((a: string) => (b: string): string => a + b))(` `))(((joinWith)(` `))(((((f: (x: any) => any) => (xs: any[]): any[] => xs.map(f)))(emitPatternAtom))(args))));
  throw new Error(`Non-exhaustive match`);
})(); }
  throw new Error(`Non-exhaustive match`);
})();

const emitPatternAtom = (p: Pattern) => (() => {
  if (p?._tag === `PCon`) {  return ((((a: string) => (b: string): string => a + b))(`(`))(((((a: string) => (b: string): string => a + b))((emitPattern)(p)))(`)`)); }
  if (p?._tag === `PCons`) {  return ((((a: string) => (b: string): string => a + b))(`(`))(((((a: string) => (b: string): string => a + b))((emitPattern)(p)))(`)`)); }
  return (emitPattern)(p);
  throw new Error(`Non-exhaustive match`);
})();

const gatherAppHead = (e: Expr) => (() => {
  if (e?._tag === `EApp`) { const f = (e as any)._0; return (gatherAppHead)(f); }
  return e;
  throw new Error(`Non-exhaustive match`);
})();

const gatherAppArgs = (e: Expr) => (() => {
  if (e?._tag === `EApp`) { const f = (e as any)._0; const a = (e as any)._1; return ((((xs: any[]) => (ys: any[]): any[] => [...xs, ...ys]))((gatherAppArgs)(f)))([a]); }
  return [];
  throw new Error(`Non-exhaustive match`);
})();

const gatherLamNames = (e: Expr) => (() => {
  if (e?._tag === `ELam`) { const name = (e as any)._0; const body = (e as any)._1; return [name, ...(gatherLamNames)(body)]; }
  return [];
  throw new Error(`Non-exhaustive match`);
})();

const gatherLamBody = (e: Expr) => (() => {
  if (e?._tag === `ELam`) { const body = (e as any)._1; return (gatherLamBody)(body); }
  return e;
  throw new Error(`Non-exhaustive match`);
})();

const emitArms = (pats: Pattern[]) => (bodies: Expr[]) => (() => {
  if (pats.length === 0) { return ``; }
  if (pats.length > 0) { const p = pats[0], restPats = pats.slice(1); return (() => {
  if (bodies.length === 0) { return ``; }
  if (bodies.length > 0) { const b = bodies[0], restBodies = bodies.slice(1); return ((arm) => ((rest) => (() => {
  if (rest === ``) { return arm; }
  return ((((a: string) => (b: string): string => a + b))(arm))(((((a: string) => (b: string): string => a + b))(`\n`))(rest));
  throw new Error(`Non-exhaustive match`);
})())(((emitArms)(restPats))(restBodies)))(((((a: string) => (b: string): string => a + b))(`  | `))(((((a: string) => (b: string): string => a + b))((emitPattern)(p)))(((((a: string) => (b: string): string => a + b))(` -> `))((emitExpr)(b))))); }
  throw new Error(`Non-exhaustive match`);
})(); }
  throw new Error(`Non-exhaustive match`);
})();

const emitExpr = (e: Expr) => (() => {
  if (e?._tag === `EInt`) { const n = (e as any)._0; return (((n: number): string => String(n)))(n); }
  if (e?._tag === `EStr`) { const s = (e as any)._0; return ((((a: string) => (b: string): string => a + b))(`"`))(((((a: string) => (b: string): string => a + b))((escapeStr)(s)))(`"`)); }
  if (e?._tag === `EBool`) { const b = (e as any)._0; return (b ? `true` : `false`); }
  if (e?._tag === `EChar`) { const c = (e as any)._0; return (emitCharLit)(c); }
  if (e?._tag === `EFloat`) { const s = (e as any)._0; return s; }
  if (e?._tag === `EVar`) { const name = (e as any)._0; return name; }
  if (e?._tag === `ECon`) { const name = (e as any)._0; return name; }
  if (e?._tag === `ENil`) { return `[]`; }
  if (e?._tag === `EApp`) {  return ((head) => ((args) => ((((a: string) => (b: string): string => a + b))((emitExprAtom)(head)))(((((a: string) => (b: string): string => a + b))(` `))(((joinWith)(` `))(((((f: (x: any) => any) => (xs: any[]): any[] => xs.map(f)))(emitExprAtom))(args)))))((gatherAppArgs)(e)))((gatherAppHead)(e)); }
  if (e?._tag === `EIf`) { const cond = (e as any)._0; const thenExpr = (e as any)._1; const elseExpr = (e as any)._2; return ((((a: string) => (b: string): string => a + b))(`if `))(((((a: string) => (b: string): string => a + b))((emitExpr)(cond)))(((((a: string) => (b: string): string => a + b))(`\n  `))(((((a: string) => (b: string): string => a + b))((emitExpr)(thenExpr)))(((((a: string) => (b: string): string => a + b))(`\nelse `))((emitExpr)(elseExpr)))))); }
  if (e?._tag === `EMatch`) { const scrut = (e as any)._0; const pats = (e as any)._1; const bodies = (e as any)._2; return ((arms) => ((((a: string) => (b: string): string => a + b))(`match `))(((((a: string) => (b: string): string => a + b))((emitExpr)(scrut)))(((((a: string) => (b: string): string => a + b))(`\n`))(arms))))(((emitArms)(pats))(bodies)); }
  if (e?._tag === `ELam`) {  return ((params) => ((body) => ((((a: string) => (b: string): string => a + b))(`\\`))(((((a: string) => (b: string): string => a + b))(((joinWith)(` `))(params)))(((((a: string) => (b: string): string => a + b))(`. `))((emitExpr)(body)))))((gatherLamBody)(e)))((gatherLamNames)(e)); }
  if (e?._tag === `ELet`) { const name = (e as any)._0; const rhs = (e as any)._1; const body = (e as any)._2; return ((((a: string) => (b: string): string => a + b))(`let `))(((((a: string) => (b: string): string => a + b))(name))(((((a: string) => (b: string): string => a + b))(` = `))(((((a: string) => (b: string): string => a + b))((emitExpr)(rhs)))(((((a: string) => (b: string): string => a + b))(`\n`))((emitExpr)(body)))))); }
  if (e?._tag === `EList`) { const items = (e as any)._0; return ((((a: string) => (b: string): string => a + b))(`[`))(((((a: string) => (b: string): string => a + b))(((joinWith)(` `))(((((f: (x: any) => any) => (xs: any[]): any[] => xs.map(f)))(emitExpr))(items))))(`]`)); }
  if (e?._tag === `EBinOp`) { const op = (e as any)._0; const lhs = (e as any)._1; const rhs = (e as any)._2; return ((((a: string) => (b: string): string => a + b))(`(`))(((((a: string) => (b: string): string => a + b))((emitExpr)(lhs)))(((((a: string) => (b: string): string => a + b))(` `))(((((a: string) => (b: string): string => a + b))(op))(((((a: string) => (b: string): string => a + b))(` `))(((((a: string) => (b: string): string => a + b))((emitExpr)(rhs)))(`)`)))))); }
  if (e?._tag === `ETuple`) { const a = (e as any)._0; const b = (e as any)._1; return ((((a: string) => (b: string): string => a + b))(`(`))(((((a: string) => (b: string): string => a + b))((emitExpr)(a)))(((((a: string) => (b: string): string => a + b))(`, `))(((((a: string) => (b: string): string => a + b))((emitExpr)(b)))(`)`)))); }
  if (e?._tag === `ECons`) { const h = (e as any)._0; const t = (e as any)._1; return ((((a: string) => (b: string): string => a + b))((emitExprAtom)(h)))(((((a: string) => (b: string): string => a + b))(` :: `))((emitExpr)(t))); }
  throw new Error(`Non-exhaustive match`);
})();

const emitDecl = (d: Decl) => (() => {
  if (d?._tag === `DFn`) { const name = (d as any)._0; const params = (d as any)._1; const body = (d as any)._3; return ((((a: string) => (b: string): string => a + b))(name))(((((a: string) => (b: string): string => a + b))((emitParams)(params)))(((((a: string) => (b: string): string => a + b))(` = `))((emitExpr)(body)))); }
  if (d?._tag === `DType`) { const name = (d as any)._0; const tvars = (d as any)._1; const ctors = (d as any)._2; return ((header) => ((((a: string) => (b: string): string => a + b))(header))(((((a: string) => (b: string): string => a + b))(` = `))(((joinWith)(` | `))(((((f: (x: any) => any) => (xs: any[]): any[] => xs.map(f)))(emitCtor))(ctors)))))((() => {
  if (tvars.length === 0) { return name; }
  return ((((a: string) => (b: string): string => a + b))(name))(((((a: string) => (b: string): string => a + b))(` `))(((joinWith)(` `))(tvars)));
  throw new Error(`Non-exhaustive match`);
})()); }
  if (d?._tag === `DImport`) { const segs = (d as any)._0; return ((((a: string) => (b: string): string => a + b))(`import `))(((joinWith)(`.`))(segs)); }
  if (d?._tag === `DExport`) { const inner = (d as any)._0; return ((((a: string) => (b: string): string => a + b))(`export `))((emitDecl)(inner)); }
  if (d?._tag === `DLet`) { const name = (d as any)._0; const body = (d as any)._1; return ((((a: string) => (b: string): string => a + b))(`let `))(((((a: string) => (b: string): string => a + b))(name))(((((a: string) => (b: string): string => a + b))(` = `))((emitExpr)(body)))); }
  throw new Error(`Non-exhaustive match`);
})();

const emitDecls = (ds: Decl[]) => ((joinWith)(`\n\n`))(((((f: (x: any) => any) => (xs: any[]): any[] => xs.map(f)))(emitDecl))(ds));

const renderModule = (m: Module) => (() => {
  if (m?._tag === `MkModule`) { const path = (m as any)._0; const decls = (m as any)._1; return (() => {
  if (path.length === 0) { return (emitDecls)(decls); }
  return ((((a: string) => (b: string): string => a + b))(`module `))(((((a: string) => (b: string): string => a + b))(((joinWith)(`.`))(path)))(((((a: string) => (b: string): string => a + b))(`\n\n`))((emitDecls)(decls))));
  throw new Error(`Non-exhaustive match`);
})(); }
  throw new Error(`Non-exhaustive match`);
})();

type ElabError = { _tag: `MkError`; _0: string };
const MkError = (_0: string): ElabError => ({ _tag: `MkError` as const, _0 });

type Env = { _tag: `MkEnv`; _0: string[] };
const MkEnv = (_0: string[]): Env => ({ _tag: `MkEnv` as const, _0 });

const errMsg = (e: ElabError) => (() => {
  if (e?._tag === `MkError`) { const msg = (e as any)._0; return msg; }
  throw new Error(`Non-exhaustive match`);
})();

const emptyEnv = () => MkEnv([]);

const envAdd = (name: string) => (env: Env) => (() => {
  if (env?._tag === `MkEnv`) { const xs = (env as any)._0; return MkEnv(((((xs: any[]) => (ys: any[]): any[] => [...xs, ...ys]))(xs))([name])); }
  throw new Error(`Non-exhaustive match`);
})();

const envAddAll = (names: string[]) => (env: Env) => (((((f: (acc: any) => (x: any) => any) => (z: any) => (xs: any[]): any => xs.reduce((a: any, x: any) => f(a)(x), z)))((acc: Env) => (n: string) => ((envAdd)(n))(acc)))(env))(names);

const strOrEq = (target: string) => (acc: boolean) => (x: string) => (acc ? true : (x === target));

const envHas = (name: string) => (env: Env) => (() => {
  if (env?._tag === `MkEnv`) { const xs = (env as any)._0; return (((((f: (acc: any) => (x: any) => any) => (z: any) => (xs: any[]): any => xs.reduce((a: any, x: any) => f(a)(x), z)))((acc: boolean) => (x: string) => (((strOrEq)(name))(acc))(x)))(false))(xs); }
  throw new Error(`Non-exhaustive match`);
})();

const patBinders = (p: Pattern) => (() => {
  if (p?._tag === `PVar`) { const name = (p as any)._0; return [name]; }
  if (p?._tag === `PWild`) { return []; }
  if (p?._tag === `PNil`) { return []; }
  if (p?._tag === `PLitInt`) {  return []; }
  if (p?._tag === `PLitStr`) {  return []; }
  if (p?._tag === `PCons`) { const h = (p as any)._0; const t = (p as any)._1; return ((((xs: any[]) => (ys: any[]): any[] => [...xs, ...ys]))((patBinders)(h)))((patBinders)(t)); }
  if (p?._tag === `PCon`) { const args = (p as any)._1; return (patBindersList)(args); }
  throw new Error(`Non-exhaustive match`);
})();

const patBindersList = (ps: Pattern[]) => (() => {
  if (ps.length === 0) { return []; }
  if (ps.length > 0) { const p = ps[0], rest = ps.slice(1); return ((((xs: any[]) => (ys: any[]): any[] => [...xs, ...ys]))((patBinders)(p)))((patBindersList)(rest)); }
  throw new Error(`Non-exhaustive match`);
})();

const paramName = (p: Param) => (() => {
  if (p?._tag === `MkParam`) { const n = (p as any)._0; return n; }
  throw new Error(`Non-exhaustive match`);
})();

const paramNames = (ps: Param[]) => ((((f: (x: any) => any) => (xs: any[]): any[] => xs.map(f)))((p: Param) => (paramName)(p)))(ps);

const conName = (c: Constructor) => (() => {
  if (c?._tag === `MkCon`) { const n = (c as any)._0; return n; }
  throw new Error(`Non-exhaustive match`);
})();

const conNames = (cs: Constructor[]) => ((((f: (x: any) => any) => (xs: any[]): any[] => xs.map(f)))((c: Constructor) => (conName)(c)))(cs);

const listContains = (name: string) => (xs: string[]) => (((((f: (acc: any) => (x: any) => any) => (z: any) => (xs: any[]): any => xs.reduce((a: any, x: any) => f(a)(x), z)))((acc: boolean) => (x: string) => (((strOrEq)(name))(acc))(x)))(false))(xs);

const findDuplicatesAcc = (xs: string[]) => (seen: string[]) => (() => {
  if (xs.length === 0) { return []; }
  if (xs.length > 0) { const x = xs[0], rest = xs.slice(1); return (((((xs: any[]) => (x: any): boolean => xs.includes(x)))(x))(seen) ? [x, ...((findDuplicatesAcc)(rest))(seen)] : ((findDuplicatesAcc)(rest))([x, ...seen])); }
  throw new Error(`Non-exhaustive match`);
})();

const findDuplicates = (xs: string[]) => ((findDuplicatesAcc)(xs))([]);

const declName = (d: Decl) => (() => {
  if (d?._tag === `DFn`) { const name = (d as any)._0; return Some(name); }
  if (d?._tag === `DLet`) { const name = (d as any)._0; return Some(name); }
  if (d?._tag === `DType`) { const name = (d as any)._0; return Some(name); }
  if (d?._tag === `DImport`) {  return None; }
  if (d?._tag === `DExport`) { const inner = (d as any)._0; return (declName)(inner); }
  throw new Error(`Non-exhaustive match`);
})();

const addDeclName = (acc: string[]) => (d: Decl) => (() => {
  if ((declName)(d)?._tag === `Some`) { const name = ((declName)(d) as any)._0; return ((((xs: any[]) => (ys: any[]): any[] => [...xs, ...ys]))(acc))([name]); }
  if ((declName)(d)?._tag === `None`) { return acc; }
  throw new Error(`Non-exhaustive match`);
})();

const collectDeclNames = (decls: Decl[]) => (((((f: (acc: any) => (x: any) => any) => (z: any) => (xs: any[]): any => xs.reduce((a: any, x: any) => f(a)(x), z)))((acc: string[]) => (d: Decl) => ((addDeclName)(acc))(d)))([]))(decls);

const collectDecl = (d: Decl) => (env: Env) => (() => {
  if (d?._tag === `DFn`) { const name = (d as any)._0; return ((envAdd)(name))(env); }
  if (d?._tag === `DLet`) { const name = (d as any)._0; return ((envAdd)(name))(env); }
  if (d?._tag === `DType`) { const ctors = (d as any)._2; return ((envAddAll)((conNames)(ctors)))(env); }
  if (d?._tag === `DImport`) {  return env; }
  if (d?._tag === `DExport`) { const inner = (d as any)._0; return ((collectDecl)(inner))(env); }
  throw new Error(`Non-exhaustive match`);
})();

const collectDecls = (decls: Decl[]) => (env: Env) => (((((f: (acc: any) => (x: any) => any) => (z: any) => (xs: any[]): any => xs.reduce((a: any, x: any) => f(a)(x), z)))((acc: Env) => (d: Decl) => ((collectDecl)(d))(acc)))(env))(decls);

const checkExpr = (env: Env) => (e: Expr) => (() => {
  if (e?._tag === `EInt`) {  return []; }
  if (e?._tag === `EStr`) {  return []; }
  if (e?._tag === `EBool`) {  return []; }
  if (e?._tag === `EChar`) {  return []; }
  if (e?._tag === `EFloat`) {  return []; }
  if (e?._tag === `ENil`) { return []; }
  if (e?._tag === `EVar`) { const name = (e as any)._0; return (((envHas)(name))(env) ? [] : [MkError(((((a: string) => (b: string): string => a + b))(`Unbound variable: `))(name))]); }
  if (e?._tag === `ECon`) { const name = (e as any)._0; return (((envHas)(name))(env) ? [] : [MkError(((((a: string) => (b: string): string => a + b))(`Unbound constructor: `))(name))]); }
  if (e?._tag === `EApp`) { const f = (e as any)._0; const x = (e as any)._1; return ((((xs: any[]) => (ys: any[]): any[] => [...xs, ...ys]))(((checkExpr)(env))(f)))(((checkExpr)(env))(x)); }
  if (e?._tag === `EBinOp`) { const l = (e as any)._1; const r = (e as any)._2; return ((((xs: any[]) => (ys: any[]): any[] => [...xs, ...ys]))(((checkExpr)(env))(l)))(((checkExpr)(env))(r)); }
  if (e?._tag === `EIf`) { const cnd = (e as any)._0; const thn = (e as any)._1; const els = (e as any)._2; return ((((xs: any[]) => (ys: any[]): any[] => [...xs, ...ys]))(((((xs: any[]) => (ys: any[]): any[] => [...xs, ...ys]))(((checkExpr)(env))(cnd)))(((checkExpr)(env))(thn))))(((checkExpr)(env))(els)); }
  if (e?._tag === `ELam`) { const param = (e as any)._0; const body = (e as any)._1; return ((env2) => ((checkExpr)(env2))(body))(((envAdd)(param))(env)); }
  if (e?._tag === `ELet`) { const name = (e as any)._0; const rhs = (e as any)._1; const body = (e as any)._2; return ((rhsErrs) => ((env2) => ((bodyErrs) => ((((xs: any[]) => (ys: any[]): any[] => [...xs, ...ys]))(rhsErrs))(bodyErrs))(((checkExpr)(env2))(body)))(((envAdd)(name))(env)))(((checkExpr)(env))(rhs)); }
  if (e?._tag === `EMatch`) { const scrut = (e as any)._0; const pats = (e as any)._1; const bodies = (e as any)._2; return ((scrutErrs) => ((armErrs) => ((((xs: any[]) => (ys: any[]): any[] => [...xs, ...ys]))(scrutErrs))(armErrs))((((checkArms)(env))(pats))(bodies)))(((checkExpr)(env))(scrut)); }
  if (e?._tag === `EList`) { const items = (e as any)._0; return (((((f: (acc: any) => (x: any) => any) => (z: any) => (xs: any[]): any => xs.reduce((a: any, x: any) => f(a)(x), z)))((acc: ElabError[]) => (item: Expr) => ((((xs: any[]) => (ys: any[]): any[] => [...xs, ...ys]))(acc))(((checkExpr)(env))(item))))([]))(items); }
  if (e?._tag === `ECons`) { const h = (e as any)._0; const t = (e as any)._1; return ((((xs: any[]) => (ys: any[]): any[] => [...xs, ...ys]))(((checkExpr)(env))(h)))(((checkExpr)(env))(t)); }
  if (e?._tag === `ETuple`) { const a = (e as any)._0; const b = (e as any)._1; return ((((xs: any[]) => (ys: any[]): any[] => [...xs, ...ys]))(((checkExpr)(env))(a)))(((checkExpr)(env))(b)); }
  throw new Error(`Non-exhaustive match`);
})();

const checkArmsCons = (env: Env) => (p: Pattern) => (restPats: Pattern[]) => (bodies: Expr[]) => (() => {
  if (bodies.length === 0) { return []; }
  if (bodies.length > 0) { const b = bodies[0], restBodies = bodies.slice(1); return ((armEnv) => ((armErrs) => ((restErrs) => ((((xs: any[]) => (ys: any[]): any[] => [...xs, ...ys]))(armErrs))(restErrs))((((checkArms)(env))(restPats))(restBodies)))(((checkExpr)(armEnv))(b)))(((envAddAll)((patBinders)(p)))(env)); }
  throw new Error(`Non-exhaustive match`);
})();

const checkArms = (env: Env) => (pats: Pattern[]) => (bodies: Expr[]) => (() => {
  if (pats.length === 0) { return []; }
  if (pats.length > 0) { const p = pats[0], restPats = pats.slice(1); return ((((checkArmsCons)(env))(p))(restPats))(bodies); }
  throw new Error(`Non-exhaustive match`);
})();

const checkDecl = (env: Env) => (d: Decl) => (() => {
  if (d?._tag === `DFn`) { const params = (d as any)._1; const body = (d as any)._3; return ((bodyEnv) => ((checkExpr)(bodyEnv))(body))(((envAddAll)((paramNames)(params)))(env)); }
  if (d?._tag === `DLet`) { const body = (d as any)._1; return ((checkExpr)(env))(body); }
  if (d?._tag === `DType`) {  return []; }
  if (d?._tag === `DImport`) {  return []; }
  if (d?._tag === `DExport`) { const inner = (d as any)._0; return ((checkDecl)(env))(inner); }
  throw new Error(`Non-exhaustive match`);
})();

const checkDecls = (env: Env) => (decls: Decl[]) => (((((f: (acc: any) => (x: any) => any) => (z: any) => (xs: any[]): any => xs.reduce((a: any, x: any) => f(a)(x), z)))((acc: ElabError[]) => (d: Decl) => ((((xs: any[]) => (ys: any[]): any[] => [...xs, ...ys]))(acc))(((checkDecl)(env))(d))))([]))(decls);

const makeDupError = (name: string) => MkError(((((a: string) => (b: string): string => a + b))(`Duplicate declaration: `))(name));

const checkDuplicates = (decls: Decl[]) => ((names) => ((dups) => ((((f: (x: any) => any) => (xs: any[]): any[] => xs.map(f)))((name: string) => (makeDupError)(name)))(dups))((findDuplicates)(names)))((collectDeclNames)(decls));

const builtinNames = () => [`abs`, ...[`absf`, ...[`sqrt`, ...[`min`, ...[`max`, ...[`listLen`, ...[`listMap`, ...[`listFilter`, ...[`listFold`, ...[`listReverse`, ...[`listAppend`, ...[`listConcat`, ...[`listIsEmpty`, ...[`listHead`, ...[`listTail`, ...[`listAt`, ...[`strLen`, ...[`strConcat`, ...[`strTrim`, ...[`strContains`, ...[`strChars`, ...[`strFromChars`, ...[`strReverse`, ...[`strSlice`, ...[`strIndexOf`, ...[`strSplit`, ...[`strToInt`, ...[`charToInt`, ...[`intToChar`, ...[`intToStr`, ...[`charIsDigit`, ...[`charIsAlpha`, ...[`charIsSpace`, ...[`print`, ...[`printfn`, ...[`readFile`, ...[`writeFile`, ...[`fileExists`, ...[`exit`, ...[`getArgs`, ...[`maybeMap`, ...[`maybeBind`, ...[`maybeWithDefault`, ...[`true`, ...[`false`, ...[]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]]];

const elaborate = (m: Module) => (() => {
  if (m?._tag === `MkModule`) { const decls = (m as any)._1; return ((builtinEnv) => ((env) => ((dupErrs) => ((bodyErrs) => ((((xs: any[]) => (ys: any[]): any[] => [...xs, ...ys]))(dupErrs))(bodyErrs))(((checkDecls)(env))(decls)))((checkDuplicates)(decls)))(((collectDecls)(decls))(builtinEnv)))(((envAddAll)(builtinNames))(emptyEnv)); }
  throw new Error(`Non-exhaustive match`);
})();

const assertNoErrors = (label: string) => (errs: ElabError[]) => ((((xs: any[]): boolean => xs.length === 0))(errs) ? (((s: string): void => { console.log(s) }))(((((a: string) => (b: string): string => a + b))(`OK `))(label)) : (((s: string): void => { console.log(s) }))(((((a: string) => (b: string): string => a + b))(`FAIL `))(((((a: string) => (b: string): string => a + b))(label))(` — unexpected errors`))));

const errContains = (needle: string) => (e: ElabError) => ((((needle: string) => (hay: string): boolean => hay.includes(needle)))(needle))((errMsg)(e));

const errOrContains = (needle: string) => (acc: boolean) => (e: ElabError) => (acc ? true : ((errContains)(needle))(e));

const hasErrorWith = (needle: string) => (errs: ElabError[]) => (((((f: (acc: any) => (x: any) => any) => (z: any) => (xs: any[]): any => xs.reduce((a: any, x: any) => f(a)(x), z)))((acc: boolean) => (e: ElabError) => (((errOrContains)(needle))(acc))(e)))(false))(errs);

const assertHasError = (label: string) => (needle: string) => (errs: ElabError[]) => (((hasErrorWith)(needle))(errs) ? (((s: string): void => { console.log(s) }))(((((a: string) => (b: string): string => a + b))(`OK `))(label)) : (((s: string): void => { console.log(s) }))(((((a: string) => (b: string): string => a + b))(`FAIL `))(((((a: string) => (b: string): string => a + b))(label))(((((a: string) => (b: string): string => a + b))(` — expected error: `))(needle)))));

const __ll_main_Std_Elaborator = () => ((colorCtors1) => ((decls1) => ((m1) => ((_) => ((decls2) => ((m2) => ((_) => ((decls3) => ((m3) => ((_) => ((matchExpr) => ((colorCtors4) => ((decls4) => ((m4) => ((_) => ((decls5) => ((m5) => ((_) => ((letExpr) => ((decls6) => ((m6) => ((_) => 0)(((assertNoErrors)(`6 let binding scoping`))((elaborate)(m6))))(MkModule([`M`, ...[]], decls6)))([DFn(`useZ`, [], None, letExpr), ...[]]))(ELet(`z`, EInt(10), EBinOp(`+`, EVar(`z`), EInt(1)))))((((assertHasError)(`5 unbound constructor`))(`Nope`))((elaborate)(m5))))(MkModule([`M`, ...[]], decls5)))([DFn(`bad`, [MkParam(`x`, TyName(`Int`)), ...[]], None, ECon(`Nope`)), ...[]]))(((assertNoErrors)(`4 valid match with constructors`))((elaborate)(m4))))(MkModule([`M`, ...[]], decls4)))([DType(`Color`, [], colorCtors4), ...[DFn(`describeColor`, [MkParam(`x`, TyName(`Color`)), ...[]], None, matchExpr), ...[]]]))([MkCon(`Red`, []), ...[MkCon(`Black`, []), ...[]]]))(EMatch(EVar(`x`), [PCon(`Red`, []), ...[PCon(`Black`, []), ...[]]], [EInt(1), ...[EInt(2), ...[]]])))((((assertHasError)(`3 duplicate function`))(`foo`))((elaborate)(m3))))(MkModule([`M`, ...[]], decls3)))([DFn(`foo`, [MkParam(`x`, TyName(`Int`)), ...[]], None, EVar(`x`)), ...[DFn(`foo`, [MkParam(`y`, TyName(`Int`)), ...[]], None, EVar(`y`)), ...[]]]))((((assertHasError)(`2 unbound variable`))(`y`))((elaborate)(m2))))(MkModule([`M`, ...[]], decls2)))([DFn(`broken`, [MkParam(`x`, TyName(`Int`)), ...[]], None, EVar(`y`)), ...[]]))(((assertNoErrors)(`1 valid program`))((elaborate)(m1))))(MkModule([`M`, ...[]], decls1)))([DType(`Color`, [], colorCtors1), ...[DFn(`id`, [MkParam(`x`, TyName(`Int`)), ...[]], None, EVar(`x`)), ...[]]]))([MkCon(`Red`, []), ...[MkCon(`Black`, []), ...[]]]);

const joinWith = (sep: string) => (items: string[]) => (() => {
  if (items.length === 0) { return ``; }
  return x;
  if (items.length > 0) { const x = items[0], rest = items.slice(1); return ((((a: string) => (b: string): string => a + b))(x))(((((a: string) => (b: string): string => a + b))(sep))(((joinWith)(sep))(rest))); }
  throw new Error(`Non-exhaustive match`);
})();

const isFsKeyword = (s: string) => (() => {
  if (s === `abstract`) { return true; }
  if (s === `and`) { return true; }
  if (s === `as`) { return true; }
  if (s === `assert`) { return true; }
  if (s === `base`) { return true; }
  if (s === `begin`) { return true; }
  if (s === `class`) { return true; }
  if (s === `default`) { return true; }
  if (s === `delegate`) { return true; }
  if (s === `do`) { return true; }
  if (s === `done`) { return true; }
  if (s === `downcast`) { return true; }
  if (s === `downto`) { return true; }
  if (s === `elif`) { return true; }
  if (s === `else`) { return true; }
  if (s === `end`) { return true; }
  if (s === `exception`) { return true; }
  if (s === `extern`) { return true; }
  if (s === `false`) { return true; }
  if (s === `finally`) { return true; }
  if (s === `for`) { return true; }
  if (s === `fun`) { return true; }
  if (s === `function`) { return true; }
  if (s === `global`) { return true; }
  if (s === `if`) { return true; }
  if (s === `in`) { return true; }
  if (s === `inherit`) { return true; }
  if (s === `inline`) { return true; }
  if (s === `interface`) { return true; }
  if (s === `internal`) { return true; }
  if (s === `let`) { return true; }
  if (s === `match`) { return true; }
  if (s === `member`) { return true; }
  if (s === `mod`) { return true; }
  if (s === `module`) { return true; }
  if (s === `mutable`) { return true; }
  if (s === `namespace`) { return true; }
  if (s === `new`) { return true; }
  if (s === `not`) { return true; }
  if (s === `null`) { return true; }
  if (s === `of`) { return true; }
  if (s === `open`) { return true; }
  if (s === `or`) { return true; }
  if (s === `override`) { return true; }
  if (s === `private`) { return true; }
  if (s === `public`) { return true; }
  if (s === `rec`) { return true; }
  if (s === `return`) { return true; }
  if (s === `static`) { return true; }
  if (s === `struct`) { return true; }
  if (s === `then`) { return true; }
  if (s === `to`) { return true; }
  if (s === `true`) { return true; }
  if (s === `try`) { return true; }
  if (s === `type`) { return true; }
  if (s === `upcast`) { return true; }
  if (s === `use`) { return true; }
  if (s === `val`) { return true; }
  if (s === `void`) { return true; }
  if (s === `when`) { return true; }
  if (s === `while`) { return true; }
  if (s === `with`) { return true; }
  if (s === `yield`) { return true; }
  if (s === `params`) { return true; }
  if (s === `object`) { return true; }
  if (s === `trait`) { return true; }
  return false;
  throw new Error(`Non-exhaustive match`);
})();

const safeIdent = (s: string) => ((isFsKeyword)(s) ? ((((a: string) => (b: string): string => a + b))(`__ll_`))(s) : s);

const encodeStrEscape = (c: string) => (() => {
  if (c === `\n`) { return `\\n`; }
  if (c === `\t`) { return `\\t`; }
  if (c === `\r`) { return `\\r`; }
  if (c === `\\`) { return `\\\\`; }
  if (c === `"`) { return `\\"`; }
  return (((cs: string[]): string => cs.join('')))([c]);
  throw new Error(`Non-exhaustive match`);
})();

const escapeStr = (s: string) => (((((f: (acc: any) => (x: any) => any) => (z: any) => (xs: any[]): any => xs.reduce((a: any, x: any) => f(a)(x), z)))((acc: string) => (c: string) => ((((a: string) => (b: string): string => a + b))(acc))((encodeStrEscape)(c))))(``))((((s: string): string[] => Array.from(s)))(s));

const mapOp = (op: string) => ((op === `==`) ? `=` : ((op === `!=`) ? `<>` : op));

const isTypeParam = (s: string) => (() => {
  return ((n) => ((n >= 65) ? ((n <= 90) ? true : false) : false))((((c: string): number => c.charCodeAt(0)))(c));
  return false;
  throw new Error(`Non-exhaustive match`);
})();

const emitType = (t: TypeExpr) => (() => {
  if (t?._tag === `TyName`) {  return `int64`; }
  if (t?._tag === `TyName`) {  return `string`; }
  if (t?._tag === `TyName`) {  return `bool`; }
  if (t?._tag === `TyName`) {  return `char`; }
  if (t?._tag === `TyName`) {  return `float`; }
  if (t?._tag === `TyName`) {  return `unit`; }
  if (t?._tag === `TyName`) { const n = (t as any)._0; return ((isTypeParam)(n) ? ((((a: string) => (b: string): string => a + b))(`'`))(n) : n); }
  if (t?._tag === `TyApp`) { const a = (t as any)._1; return ((((a: string) => (b: string): string => a + b))((emitType)(a)))(` list`); }
  if (t?._tag === `TyApp`) { const f = (t as any)._0; const a = (t as any)._1; return ((head) => ((args) => (() => {
  return ((((a: string) => (b: string): string => a + b))((emitType)(a)))(((((a: string) => (b: string): string => a + b))(` `))((emitType)(head)));
  return ((inner) => ((((a: string) => (b: string): string => a + b))((emitType)(head)))(((((a: string) => (b: string): string => a + b))(`<`))(((((a: string) => (b: string): string => a + b))(inner))(`>`))))(((joinWith)(`, `))(((((f: (x: any) => any) => (xs: any[]): any[] => xs.map(f)))(emitType))(args)));
  throw new Error(`Non-exhaustive match`);
})())((collectTyAppArgs)(TyApp(f, a))))((collectTyAppHead)(TyApp(f, a))); }
  if (t?._tag === `TyFn`) { const a = (t as any)._0; const b = (t as any)._1; return ((((a: string) => (b: string): string => a + b))((emitType)(a)))(((((a: string) => (b: string): string => a + b))(` -> `))((emitType)(b))); }
  throw new Error(`Non-exhaustive match`);
})();

const collectTyAppHead = (t: TypeExpr) => (() => {
  if (t?._tag === `TyApp`) { const f = (t as any)._0; return (collectTyAppHead)(f); }
  return t;
  throw new Error(`Non-exhaustive match`);
})();

const collectTyAppArgs = (t: TypeExpr) => (() => {
  if (t?._tag === `TyApp`) { const f = (t as any)._0; const a = (t as any)._1; return ((((xs: any[]) => (ys: any[]): any[] => [...xs, ...ys]))((collectTyAppArgs)(f)))([a]); }
  return [];
  throw new Error(`Non-exhaustive match`);
})();

const emitPattern = (p: Pattern) => (() => {
  if (p?._tag === `PVar`) { const x = (p as any)._0; return (safeIdent)(x); }
  if (p?._tag === `PWild`) { return `_`; }
  if (p?._tag === `PNil`) { return `[]`; }
  if (p?._tag === `PLitInt`) { const n = (p as any)._0; return ((((a: string) => (b: string): string => a + b))((((n: number): string => String(n)))(n)))(`L`); }
  if (p?._tag === `PLitStr`) { const s = (p as any)._0; return ((((a: string) => (b: string): string => a + b))(`"`))(((((a: string) => (b: string): string => a + b))((escapeStr)(s)))(`"`)); }
  if (p?._tag === `PCons`) { const h = (p as any)._0; const t = (p as any)._1; return ((((a: string) => (b: string): string => a + b))(`(`))(((((a: string) => (b: string): string => a + b))((emitPattern)(h)))(((((a: string) => (b: string): string => a + b))(` :: `))(((((a: string) => (b: string): string => a + b))((emitPattern)(t)))(`)`)))); }
  if (p?._tag === `PCon`) { const c = (p as any)._0; const args = (p as any)._1; return ((emitConPattern)(c))(args); }
  throw new Error(`Non-exhaustive match`);
})();

const emitConPattern = (c: string) => (args: Pattern[]) => (() => {
  if (args.length === 0) { return c; }
  return ((inner) => ((((a: string) => (b: string): string => a + b))(c))(((((a: string) => (b: string): string => a + b))(`(`))(((((a: string) => (b: string): string => a + b))(inner))(`)`))))((emitPattern)((patListHead)(args)));
  return ((inner) => ((((a: string) => (b: string): string => a + b))(c))(((((a: string) => (b: string): string => a + b))(`(`))(((((a: string) => (b: string): string => a + b))(inner))(`)`))))(((joinWith)(`, `))(((((f: (x: any) => any) => (xs: any[]): any[] => xs.map(f)))(emitPattern))(args)));
  throw new Error(`Non-exhaustive match`);
})();

const patListHead = (xs: Pattern[]) => (() => {
  return x;
  return PWild;
  throw new Error(`Non-exhaustive match`);
})();

const emitCharLit = (c: string) => ((c === `\n`) ? `'\\n'` : ((c === `\t`) ? `'\\t'` : ((c === `\\`) ? `'\\\\'` : ((c === `'`) ? `'\\''` : ((((a: string) => (b: string): string => a + b))(((((a: string) => (b: string): string => a + b))(`'`))((((cs: string[]): string => cs.join('')))([c]))))(`'`)))));

const emitExpr = (e: Expr) => (() => {
  if (e?._tag === `EInt`) { const n = (e as any)._0; return ((((a: string) => (b: string): string => a + b))((((n: number): string => String(n)))(n)))(`L`); }
  if (e?._tag === `EStr`) { const s = (e as any)._0; return ((((a: string) => (b: string): string => a + b))(`"`))(((((a: string) => (b: string): string => a + b))((escapeStr)(s)))(`"`)); }
  if (e?._tag === `EBool`) { const b = (e as any)._0; return (b ? `true` : `false`); }
  if (e?._tag === `EChar`) { const c = (e as any)._0; return (emitCharLit)(c); }
  if (e?._tag === `EFloat`) { const s = (e as any)._0; return s; }
  if (e?._tag === `EVar`) { const x = (e as any)._0; return (safeIdent)(x); }
  if (e?._tag === `ECon`) { const c = (e as any)._0; return c; }
  if (e?._tag === `ENil`) { return `[]`; }
  if (e?._tag === `EApp`) { const f = (e as any)._0; const a = (e as any)._1; return ((emitApp)(f))(a); }
  if (e?._tag === `EIf`) { const c = (e as any)._0; const t = (e as any)._1; const el = (e as any)._2; return ((((a: string) => (b: string): string => a + b))(`(if `))(((((a: string) => (b: string): string => a + b))((emitExpr)(c)))(((((a: string) => (b: string): string => a + b))(` then `))(((((a: string) => (b: string): string => a + b))((emitExpr)(t)))(((((a: string) => (b: string): string => a + b))(` else `))(((((a: string) => (b: string): string => a + b))((emitExpr)(el)))(`)`)))))); }
  if (e?._tag === `EMatch`) { const scrut = (e as any)._0; const pats = (e as any)._1; const bodies = (e as any)._2; return ((arms) => ((((a: string) => (b: string): string => a + b))(`(match `))(((((a: string) => (b: string): string => a + b))((emitExpr)(scrut)))(((((a: string) => (b: string): string => a + b))(` with `))(((((a: string) => (b: string): string => a + b))(arms))(`)`)))))(((emitArms)(pats))(bodies)); }
  if (e?._tag === `ELam`) { const x = (e as any)._0; const body = (e as any)._1; return ((((a: string) => (b: string): string => a + b))(`(fun `))(((((a: string) => (b: string): string => a + b))((safeIdent)(x)))(((((a: string) => (b: string): string => a + b))(` -> `))(((((a: string) => (b: string): string => a + b))((emitExpr)(body)))(`)`)))); }
  if (e?._tag === `ELet`) { const x = (e as any)._0; const e = (e as any)._1; const body = (e as any)._2; return ((((a: string) => (b: string): string => a + b))(`(let `))(((((a: string) => (b: string): string => a + b))((safeIdent)(x)))(((((a: string) => (b: string): string => a + b))(` = `))(((((a: string) => (b: string): string => a + b))((emitExpr)(e)))(((((a: string) => (b: string): string => a + b))(` in `))(((((a: string) => (b: string): string => a + b))((emitExpr)(body)))(`)`)))))); }
  if (e?._tag === `EList`) { const items = (e as any)._0; return ((((a: string) => (b: string): string => a + b))(`[`))(((((a: string) => (b: string): string => a + b))(((joinWith)(`; `))(((((f: (x: any) => any) => (xs: any[]): any[] => xs.map(f)))(emitExpr))(items))))(`]`)); }
  if (e?._tag === `EBinOp`) { const op = (e as any)._0; const l = (e as any)._1; const r = (e as any)._2; return ((((a: string) => (b: string): string => a + b))(`(`))(((((a: string) => (b: string): string => a + b))((emitExpr)(l)))(((((a: string) => (b: string): string => a + b))(` `))(((((a: string) => (b: string): string => a + b))((mapOp)(op)))(((((a: string) => (b: string): string => a + b))(` `))(((((a: string) => (b: string): string => a + b))((emitExpr)(r)))(`)`)))))); }
  if (e?._tag === `ETuple`) { const a = (e as any)._0; const b = (e as any)._1; return ((((a: string) => (b: string): string => a + b))(`(`))(((((a: string) => (b: string): string => a + b))((emitExpr)(a)))(((((a: string) => (b: string): string => a + b))(`, `))(((((a: string) => (b: string): string => a + b))((emitExpr)(b)))(`)`)))); }
  if (e?._tag === `ECons`) { const h = (e as any)._0; const t = (e as any)._1; return ((((a: string) => (b: string): string => a + b))(`(`))(((((a: string) => (b: string): string => a + b))((emitExpr)(h)))(((((a: string) => (b: string): string => a + b))(` :: `))(((((a: string) => (b: string): string => a + b))((emitExpr)(t)))(`)`)))); }
  throw new Error(`Non-exhaustive match`);
})();

const gatherAppHead = (e: Expr) => (() => {
  if (e?._tag === `EApp`) { const f = (e as any)._0; return (gatherAppHead)(f); }
  return e;
  throw new Error(`Non-exhaustive match`);
})();

const gatherAppArgs = (e: Expr) => (() => {
  if (e?._tag === `EApp`) { const f = (e as any)._0; const a = (e as any)._1; return ((((xs: any[]) => (ys: any[]): any[] => [...xs, ...ys]))((gatherAppArgs)(f)))([a]); }
  return [];
  throw new Error(`Non-exhaustive match`);
})();

const isUpperStart = (s: string) => (() => {
  return ((n) => ((n >= 65) ? ((n <= 90) ? true : false) : false))((((c: string): number => c.charCodeAt(0)))(c));
  return false;
  throw new Error(`Non-exhaustive match`);
})();

const emitApp = (f: Expr) => (a: Expr) => ((head) => ((args) => (() => {
  if (head?._tag === `ECon`) { const c = (head as any)._0; return ((emitConApp)(c))(args); }
  if (head?._tag === `EVar`) { const x = (head as any)._0; return ((isUpperStart)(x) ? ((emitConApp)(x))(args) : ((((a: string) => (b: string): string => a + b))(`(`))(((((a: string) => (b: string): string => a + b))((emitExpr)(f)))(((((a: string) => (b: string): string => a + b))(` `))(((((a: string) => (b: string): string => a + b))((emitExpr)(a)))(`)`))))); }
  return ((((a: string) => (b: string): string => a + b))(`(`))(((((a: string) => (b: string): string => a + b))((emitExpr)(f)))(((((a: string) => (b: string): string => a + b))(` `))(((((a: string) => (b: string): string => a + b))((emitExpr)(a)))(`)`))));
  throw new Error(`Non-exhaustive match`);
})())((gatherAppArgs)(EApp(f, a))))((gatherAppHead)(EApp(f, a)));

const exprListHead = (xs: Expr[]) => (() => {
  return x;
  return ENil;
  throw new Error(`Non-exhaustive match`);
})();

const emitConApp = (c: string) => (args: Expr[]) => (() => {
  if (args.length === 0) { return c; }
  return ((((a: string) => (b: string): string => a + b))(`(`))(((((a: string) => (b: string): string => a + b))(c))(((((a: string) => (b: string): string => a + b))(` `))(((((a: string) => (b: string): string => a + b))((emitExpr)((exprListHead)(args))))(`)`))));
  return ((inner) => ((((a: string) => (b: string): string => a + b))(`(`))(((((a: string) => (b: string): string => a + b))(c))(((((a: string) => (b: string): string => a + b))(` (`))(((((a: string) => (b: string): string => a + b))(inner))(`))`)))))(((joinWith)(`, `))(((((f: (x: any) => any) => (xs: any[]): any[] => xs.map(f)))(emitExpr))(args)));
  throw new Error(`Non-exhaustive match`);
})();

const emitArms = (pats: Pattern[]) => (bodies: Expr[]) => (() => {
  if (pats.length > 0) { const p = pats[0], prest = pats.slice(1); return (() => {
  if (bodies.length > 0) { const b = bodies[0], brest = bodies.slice(1); return ((arm) => ((rest) => (((((s: string): number => s.length))(rest) === 0) ? arm : ((((a: string) => (b: string): string => a + b))(arm))(((((a: string) => (b: string): string => a + b))(` `))(rest))))(((emitArms)(prest))(brest)))(((((a: string) => (b: string): string => a + b))(`| `))(((((a: string) => (b: string): string => a + b))((emitPattern)(p)))(((((a: string) => (b: string): string => a + b))(` -> `))((emitExpr)(b))))); }
  return ``;
  throw new Error(`Non-exhaustive match`);
})(); }
  return ``;
  throw new Error(`Non-exhaustive match`);
})();

const emitTypeParam = (s: string) => ((((a: string) => (b: string): string => a + b))(`'`))(s);

const emitTypeParams = (tvars: string[]) => (() => {
  if (tvars.length === 0) { return ``; }
  return ((inner) => ((((a: string) => (b: string): string => a + b))(`<`))(((((a: string) => (b: string): string => a + b))(inner))(`>`)))(((joinWith)(`, `))(((((f: (x: any) => any) => (xs: any[]): any[] => xs.map(f)))(emitTypeParam))(tvars)));
  throw new Error(`Non-exhaustive match`);
})();

const emitCtorArgs = (args: TypeExpr[]) => ((joinWith)(` * `))(((((f: (x: any) => any) => (xs: any[]): any[] => xs.map(f)))(emitType))(args));

const emitCtor = (c: Constructor) => (() => {
  if (c?._tag === `MkCon`) { const name = (c as any)._0; const args = (c as any)._1; return (() => {
  if (args.length === 0) { return ((((a: string) => (b: string): string => a + b))(`    | `))(name); }
  return ((((a: string) => (b: string): string => a + b))(`    | `))(((((a: string) => (b: string): string => a + b))(name))(((((a: string) => (b: string): string => a + b))(` of `))((emitCtorArgs)(args))));
  throw new Error(`Non-exhaustive match`);
})(); }
  throw new Error(`Non-exhaustive match`);
})();

const emitCtors = (cs: Constructor[]) => ((joinWith)(`\n`))(((((f: (x: any) => any) => (xs: any[]): any[] => xs.map(f)))(emitCtor))(cs));

const emitParamName = (p: Param) => (() => {
  if (p?._tag === `MkParam`) { const n = (p as any)._0; return (safeIdent)(n); }
  throw new Error(`Non-exhaustive match`);
})();

const emitParamStr = (ps: Param[]) => (() => {
  if (ps.length === 0) { return ``; }
  return ((((a: string) => (b: string): string => a + b))(` `))(((joinWith)(` `))(((((f: (x: any) => any) => (xs: any[]): any[] => xs.map(f)))(emitParamName))(ps)));
  throw new Error(`Non-exhaustive match`);
})();

const emitDecl = (d: Decl) => (() => {
  if (d?._tag === `DType`) { const name = (d as any)._0; const tvars = (d as any)._1; const ctors = (d as any)._2; return ((header) => ((body) => ((((a: string) => (b: string): string => a + b))(header))(((((a: string) => (b: string): string => a + b))(` =\n`))(body)))((emitCtors)(ctors)))(((((a: string) => (b: string): string => a + b))(`type `))(((((a: string) => (b: string): string => a + b))(name))((emitTypeParams)(tvars)))); }
  if (d?._tag === `DFn`) { const name = (d as any)._0; const params = (d as any)._1; const body = (d as any)._3; return ((paramStr) => ((((a: string) => (b: string): string => a + b))(`let rec `))(((((a: string) => (b: string): string => a + b))((safeIdent)(name)))(((((a: string) => (b: string): string => a + b))(paramStr))(((((a: string) => (b: string): string => a + b))(` =\n    `))((emitExpr)(body))))))((emitParamStr)(params)); }
  if (d?._tag === `DLet`) { const name = (d as any)._0; const body = (d as any)._1; return ((((a: string) => (b: string): string => a + b))(`let `))(((((a: string) => (b: string): string => a + b))((safeIdent)(name)))(((((a: string) => (b: string): string => a + b))(` = `))((emitExpr)(body)))); }
  if (d?._tag === `DImport`) { const parts = (d as any)._0; return ((((a: string) => (b: string): string => a + b))(`// import `))(((joinWith)(`.`))(parts)); }
  if (d?._tag === `DExport`) { const inner = (d as any)._0; return (emitDecl)(inner); }
  throw new Error(`Non-exhaustive match`);
})();

const emitDecls = (ds: Decl[]) => ((joinWith)(`\n\n`))(((((f: (x: any) => any) => (xs: any[]): any[] => xs.map(f)))(emitDecl))(ds));

const emitPrelude = () => `// --- ll-lang stdlib prelude (auto-generated) ---\nlet listLen (xs: 'a list) : int64 = int64 (List.length xs)\nlet listMap f xs = List.map f xs\nlet listFilter p xs = List.filter p xs\nlet listFold f z xs = List.fold f z xs\nlet listReverse xs = List.rev xs\nlet listAppend xs ys = List.append xs ys\nlet listConcat xss = List.concat xss\nlet listIsEmpty xs = List.isEmpty xs\nlet strLen (s: string) : int64 = int64 s.Length\nlet strConcat (a: string) (b: string) = a + b\nlet strChars (s: string) = s |> Seq.toList\nlet strFromChars (cs: char list) = System.String(cs |> List.toArray)\nlet intToStr (n: int64) = string n\nlet charToInt (c: char) = int64 (int c)\nlet printfn (s: string) = System.Console.WriteLine(s)\nlet print (s: string) = System.Console.Write(s)\nlet listHead xs = match xs with [] -> None | x :: _ -> Some x\nlet listTail xs = match xs with [] -> None | _ :: t -> Some t\n// --- end prelude ---`;

const emitModulePath = (parts: string[]) => ((joinWith)(`.`))(parts);

const emitModule = (m: Module) => (() => {
  if (m?._tag === `MkModule`) { const path = (m as any)._0; const decls = (m as any)._1; return ((header) => ((prelude) => ((body) => ((((a: string) => (b: string): string => a + b))(header))(((((a: string) => (b: string): string => a + b))(`\n\n`))(((((a: string) => (b: string): string => a + b))(prelude))(((((a: string) => (b: string): string => a + b))(`\n\n`))(body)))))((emitDecls)(decls)))(emitPrelude))(((((a: string) => (b: string): string => a + b))(`module `))((emitModulePath)(path))); }
  throw new Error(`Non-exhaustive match`);
})();

const codegenCheck = (label: string) => (got: string) => (expected: string) => ((got === expected) ? (((s: string): void => { console.log(s) }))(((((a: string) => (b: string): string => a + b))(`OK `))(label)) : (((s: string): void => { console.log(s) }))(((((a: string) => (b: string): string => a + b))(`FAIL `))(((((a: string) => (b: string): string => a + b))(label))(((((a: string) => (b: string): string => a + b))(`\n  got:      `))(((((a: string) => (b: string): string => a + b))(got))(((((a: string) => (b: string): string => a + b))(`\n  expected: `))(expected)))))));

const checkContains = (label: string) => (got: string) => (needle: string) => (((((needle: string) => (hay: string): boolean => hay.includes(needle)))(needle))(got) ? (((s: string): void => { console.log(s) }))(((((a: string) => (b: string): string => a + b))(`OK `))(label)) : (((s: string): void => { console.log(s) }))(((((a: string) => (b: string): string => a + b))(`FAIL `))(((((a: string) => (b: string): string => a + b))(label))(((((a: string) => (b: string): string => a + b))(`\n  missing: `))(((((a: string) => (b: string): string => a + b))(needle))(((((a: string) => (b: string): string => a + b))(`\n  in: `))(got)))))));

const __ll_main_Std_Codegen = () => ((_) => ((_) => ((_) => ((_) => ((_) => ((_) => ((_) => ((_) => ((_) => ((colorCtors) => ((colorDecl) => ((_) => ((addParams) => ((addDecl) => ((_) => ((idParams) => ((modDecls) => ((m) => ((out) => ((_) => ((_) => ((_) => ((_) => ((_) => ((_) => ((twoItems) => ((_) => ((_) => ((_) => ((_) => ((_) => 0)((((codegenCheck)(`20 TyFn`))((emitType)(TyFn(TyName(`Int`), TyName(`Bool`)))))(`int64 -> bool`)))((((codegenCheck)(`19 safeIdent foo`))((safeIdent)(`foo`)))(`foo`)))((((codegenCheck)(`18 safeIdent let`))((safeIdent)(`let`)))(`__ll_let`)))((((codegenCheck)(`17 ETuple`))((emitExpr)(ETuple(EVar(`a`), EVar(`b`)))))(`(a, b)`)))((((codegenCheck)(`16 EList`))((emitExpr)(EList(twoItems))))(`[1L; 2L]`)))([EInt(1), ...[EInt(2), ...[]]]))((((codegenCheck)(`15 ELam`))((emitExpr)(ELam(`x`, EVar(`x`)))))(`(fun x -> x)`)))((((codegenCheck)(`14 PNil`))((emitPattern)(PNil)))(`[]`)))((((codegenCheck)(`13 PWild`))((emitPattern)(PWild)))(`_`)))((((checkContains)(`12 module fn`))(out))(`let rec id`)))((((checkContains)(`12 module type`))(out))(`type Color`)))((((checkContains)(`12 module header`))(out))(`module Test.Mod`)))((emitModule)(m)))(MkModule([`Test`, ...[`Mod`, ...[]]], modDecls)))([DType(`Color`, [], colorCtors), ...[DFn(`id`, idParams, None, EVar(`x`)), ...[]]]))([MkParam(`x`, TyName(`Int`)), ...[]]))((((checkContains)(`11 DFn add`))((emitDecl)(addDecl)))(`let rec add`)))(DFn(`add`, addParams, None, EBinOp(`+`, EVar(`x`), EVar(`y`)))))([MkParam(`x`, TyName(`Int`)), ...[MkParam(`y`, TyName(`Int`)), ...[]]]))((((checkContains)(`10 DType Color`))((emitDecl)(colorDecl)))(`type Color`)))(DType(`Color`, [], colorCtors)))([MkCon(`Red`, []), ...[MkCon(`Blue`, []), ...[]]]))((((codegenCheck)(`9 TyApp List Int`))((emitType)(TyApp(TyName(`List`), TyName(`Int`)))))(`int64 list`)))((((codegenCheck)(`8 TyName Int`))((emitType)(TyName(`Int`))))(`int64`)))((((codegenCheck)(`7 PCon Some v`))((emitPattern)(PCon(`Some`, [PVar(`v`), ...[]]))))(`Some(v)`)))((((codegenCheck)(`6 ELet`))((emitExpr)(ELet(`x`, EInt(1), EVar(`x`)))))(`(let x = 1L in x)`)))((((codegenCheck)(`5 EIf`))((emitExpr)(EIf(EBool(true), EInt(1), EInt(0)))))(`(if true then 1L else 0L)`)))((((codegenCheck)(`4 EApp f x`))((emitExpr)(EApp(EVar(`f`), EVar(`x`)))))(`(f x)`)))((((codegenCheck)(`3 EBinOp +`))((emitExpr)(EBinOp(`+`, EInt(1), EInt(2)))))(`(1L + 2L)`)))((((codegenCheck)(`2 EStr hello`))((emitExpr)(EStr(`hello`))))(`"hello"`)))((((codegenCheck)(`1 EInt 42`))((emitExpr)(EInt(42))))(`42L`));

const showErrors = (errs: ElabError[]) => (((((f: (acc: any) => (x: any) => any) => (z: any) => (xs: any[]): any => xs.reduce((a: any, x: any) => f(a)(x), z)))((acc: string) => (e: ElabError) => ((msg) => (((((s: string): number => s.length))(acc) === 0) ? msg : ((((a: string) => (b: string): string => a + b))(acc))(((((a: string) => (b: string): string => a + b))(`\n`))(msg))))((errMsg)(e))))(``))(errs);

const encodeJsonEscape = (c: string) => (() => {
  if (c === `\n`) { return `\\n`; }
  if (c === `\t`) { return `\\t`; }
  if (c === `\r`) { return `\\r`; }
  if (c === `\\`) { return `\\\\`; }
  if (c === `"`) { return `\\"`; }
  return (((cs: string[]): string => cs.join('')))([c]);
  throw new Error(`Non-exhaustive match`);
})();

const escapeJson = (s: string) => (((((f: (acc: any) => (x: any) => any) => (z: any) => (xs: any[]): any => xs.reduce((a: any, x: any) => f(a)(x), z)))((acc: string) => (c: string) => ((((a: string) => (b: string): string => a + b))(acc))((encodeJsonEscape)(c))))(``))((((s: string): string[] => Array.from(s)))(s));

const jsonStr = (s: string) => ((((a: string) => (b: string): string => a + b))(`"`))(((((a: string) => (b: string): string => a + b))((escapeJson)(s)))(`"`));

const firstError = (errs: ElabError[]) => (() => {
  return (errMsg)(err);
  if (errs.length === 0) { return ``; }
  throw new Error(`Non-exhaustive match`);
})();

const secondaryErrorCount = (errs: ElabError[]) => (() => {
  return (((xs: any[]): number => xs.length))(rest);
  if (errs.length === 0) { return 0; }
  throw new Error(`Non-exhaustive match`);
})();

const declPrimaryName = (d: Decl) => (() => {
  if (d?._tag === `DFn`) { const name = (d as any)._0; return name; }
  if (d?._tag === `DType`) { const name = (d as any)._0; return name; }
  if (d?._tag === `DLet`) { const name = (d as any)._0; return name; }
  if (d?._tag === `DImport`) { const segs = (d as any)._0; return (((((f: (acc: any) => (x: any) => any) => (z: any) => (xs: any[]): any => xs.reduce((a: any, x: any) => f(a)(x), z)))((acc: string) => (seg: string) => (((((s: string): number => s.length))(acc) === 0) ? seg : ((((a: string) => (b: string): string => a + b))(acc))(((((a: string) => (b: string): string => a + b))(`.`))(seg)))))(``))(segs); }
  if (d?._tag === `DExport`) { const inner = (d as any)._0; return (declPrimaryName)(inner); }
  throw new Error(`Non-exhaustive match`);
})();

const declKindTag = (d: Decl) => (() => {
  if (d?._tag === `DFn`) {  return `fn`; }
  if (d?._tag === `DType`) {  return `type`; }
  if (d?._tag === `DLet`) {  return `let`; }
  if (d?._tag === `DImport`) {  return `import`; }
  if (d?._tag === `DExport`) { const inner = (d as any)._0; return (declKindTag)(inner); }
  throw new Error(`Non-exhaustive match`);
})();

const findSymbolKind = (name: string) => (decls: Decl[]) => (() => {
  if (decls.length === 0) { return ``; }
  if (decls.length > 0) { const d = decls[0], rest = decls.slice(1); return (((declPrimaryName)(d) === name) ? (declKindTag)(d) : ((findSymbolKind)(name))(rest)); }
  throw new Error(`Non-exhaustive match`);
})();

const hasLexErrors = (tokens: Token[]) => (() => {
  return true;
  return (hasLexErrors)(rest);
  if (tokens.length === 0) { return false; }
  throw new Error(`Non-exhaustive match`);
})();

const firstLexError = (tokens: Token[]) => (() => {
  return ((((a: string) => (b: string): string => a + b))(`Unexpected char: `))((((cs: string[]): string => cs.join('')))([c]));
  return (firstLexError)(rest);
  if (tokens.length === 0) { return ``; }
  throw new Error(`Non-exhaustive match`);
})();

const countAllTokens = (tokens: Token[]) => (() => {
  return (1 + (countAllTokens)(rest));
  if (tokens.length === 0) { return 0; }
  throw new Error(`Non-exhaustive match`);
})();

const countCodeTokens = (tokens: Token[]) => (() => {
  if (tokens.length === 0) { return 0; }
  return (countCodeTokens)(rest);
  return (countCodeTokens)(rest);
  return (countCodeTokens)(rest);
  return (countCodeTokens)(rest);
  return (1 + (countCodeTokens)(rest));
  throw new Error(`Non-exhaustive match`);
})();

const checkCompact = (src: string) => ((tokens) => ((hasLexErrors)(tokens) ? ((p1) => ((p2) => ((((a: string) => (b: string): string => a + b))(p2))(`,"secondary_count":0}`))(((((a: string) => (b: string): string => a + b))(p1))((jsonStr)((firstLexError)(tokens)))))(`{"ok":false,"stage":"lexer","primary_error":`) : ((ast) => ((errs) => (() => {
  if (errs.length === 0) { return `{"ok":true,"stage":"ok","primary_error":"","secondary_count":0}`; }
  return ((p1) => ((p2) => ((p3) => ((((a: string) => (b: string): string => a + b))(p3))(((((a: string) => (b: string): string => a + b))((((n: number): string => String(n)))((secondaryErrorCount)(errs))))(`}`)))(((((a: string) => (b: string): string => a + b))(p2))(`,"secondary_count":`)))(((((a: string) => (b: string): string => a + b))(p1))((jsonStr)((firstError)(errs)))))(`{"ok":false,"stage":"elaborate","primary_error":`);
  throw new Error(`Non-exhaustive match`);
})())((elaborate)(ast)))((parseModule)(tokens))))((tokenize)(src));

const nextBlocker = (src: string) => ((tokens) => ((hasLexErrors)(tokens) ? ((p1) => ((p2) => ((((a: string) => (b: string): string => a + b))(p2))(`,"secondary_count":0}`))(((((a: string) => (b: string): string => a + b))(p1))((jsonStr)((firstLexError)(tokens)))))(`{"ok":false,"blocker":"lexer","stage":"lexer","primary_error":`) : ((ast) => ((errs) => (() => {
  if (errs.length === 0) { return `{"ok":true,"blocker":"ok","stage":"ok","primary_error":"","secondary_count":0}`; }
  return ((p1) => ((p2) => ((p3) => ((((a: string) => (b: string): string => a + b))(p3))(((((a: string) => (b: string): string => a + b))((((n: number): string => String(n)))((secondaryErrorCount)(errs))))(`}`)))(((((a: string) => (b: string): string => a + b))(p2))(`,"secondary_count":`)))(((((a: string) => (b: string): string => a + b))(p1))((jsonStr)((firstError)(errs)))))(`{"ok":false,"blocker":"elaborate","stage":"elaborate","primary_error":`);
  throw new Error(`Non-exhaustive match`);
})())((elaborate)(ast)))((parseModule)(tokens))))((tokenize)(src));

const lookupSymbol = (src: string) => (name: string) => ((tokens) => ((hasLexErrors)(tokens) ? ((p1) => ((p2) => ((p3) => ((p4) => ((((a: string) => (b: string): string => a + b))(p4))(`,"secondary_count":0}`))(((((a: string) => (b: string): string => a + b))(p3))((jsonStr)((firstLexError)(tokens)))))(((((a: string) => (b: string): string => a + b))(p2))(`,"primary_error":`)))(((((a: string) => (b: string): string => a + b))(p1))((jsonStr)(name))))(`{"ok":false,"stage":"lexer","symbol":`) : ((ast) => (() => {
  if (ast?._tag === `MkModule`) { const decls = (ast as any)._1; return ((kind) => (((((s: string): number => s.length))(kind) === 0) ? ((p1) => ((p2) => ((p3) => ((p4) => ((((a: string) => (b: string): string => a + b))(p4))(`,"secondary_count":0}`))(((((a: string) => (b: string): string => a + b))(p3))((jsonStr)(((((a: string) => (b: string): string => a + b))(`Symbol not found: `))(name)))))(((((a: string) => (b: string): string => a + b))(p2))(`,"primary_error":`)))(((((a: string) => (b: string): string => a + b))(p1))((jsonStr)(name))))(`{"ok":false,"stage":"lookup","symbol":`) : ((p1) => ((p2) => ((p3) => ((p4) => ((((a: string) => (b: string): string => a + b))(p4))(`}`))(((((a: string) => (b: string): string => a + b))(p3))((jsonStr)(kind))))(((((a: string) => (b: string): string => a + b))(p2))(`,"kind":`)))(((((a: string) => (b: string): string => a + b))(p1))((jsonStr)(name))))(`{"ok":true,"stage":"lookup","symbol":`)))(((findSymbolKind)(name))(decls)); }
  throw new Error(`Non-exhaustive match`);
})())((parseModule)(tokens))))((tokenize)(src));

const compileCompact = (src: string) => ((tokens) => ((hasLexErrors)(tokens) ? (checkCompact)(src) : ((ast) => ((errs) => (() => {
  if (errs.length === 0) { return ((out) => ((p1) => ((p2) => ((((a: string) => (b: string): string => a + b))(p2))(`}`))(((((a: string) => (b: string): string => a + b))(p1))((((n: number): string => String(n)))((((s: string): number => s.length))(out)))))(`{"ok":true,"stage":"codegen","output_bytes":`))((emitModule)(ast)); }
  return (checkCompact)(src);
  throw new Error(`Non-exhaustive match`);
})())((elaborate)(ast)))((parseModule)(tokens))))((tokenize)(src));

const renderCompact = (src: string) => ((tokens) => ((ast) => (renderModule)(ast))((parseModule)(tokens)))((tokenize)(src));

const tokenEstimate = (src: string) => ((tokens) => ((p1) => ((p2) => ((p3) => ((p4) => ((p5) => ((((a: string) => (b: string): string => a + b))(p5))(((((a: string) => (b: string): string => a + b))((((n: number): string => String(n)))((countCodeTokens)(tokens))))(`}`)))(((((a: string) => (b: string): string => a + b))(p4))(`,"non_layout_tokens":`)))(((((a: string) => (b: string): string => a + b))(p3))((((n: number): string => String(n)))((countAllTokens)(tokens)))))(((((a: string) => (b: string): string => a + b))(p2))(`,"lexer_tokens":`)))(((((a: string) => (b: string): string => a + b))(p1))((((n: number): string => String(n)))((((s: string): number => s.length))(src)))))(`{"ok":true,"source_bytes":`))((tokenize)(src));

const compile = (src: string) => ((tokens) => ((ast) => ((errors) => (() => {
  if (errors.length === 0) { return (emitModule)(ast); }
  return (showErrors)(errors);
  throw new Error(`Non-exhaustive match`);
})())((elaborate)(ast)))((parseModule)(tokens)))((tokenize)(src));

const compilerCheckContains = (label: string) => (got: string) => (needle: string) => (((((needle: string) => (hay: string): boolean => hay.includes(needle)))(needle))(got) ? (((s: string): void => { console.log(s) }))(((((a: string) => (b: string): string => a + b))(`OK `))(label)) : (((s: string): void => { console.log(s) }))(((((a: string) => (b: string): string => a + b))(`FAIL `))(((((a: string) => (b: string): string => a + b))(label))(((((a: string) => (b: string): string => a + b))(`\n  missing:  `))(((((a: string) => (b: string): string => a + b))(needle))(((((a: string) => (b: string): string => a + b))(`\n  in output:\n`))(got)))))));

const checkHasError = (label: string) => (got: string) => (needle: string) => (((((needle: string) => (hay: string): boolean => hay.includes(needle)))(needle))(got) ? (((s: string): void => { console.log(s) }))(((((a: string) => (b: string): string => a + b))(`OK `))(label)) : (((s: string): void => { console.log(s) }))(((((a: string) => (b: string): string => a + b))(`FAIL `))(((((a: string) => (b: string): string => a + b))(label))(((((a: string) => (b: string): string => a + b))(`\n  expected error containing: `))(((((a: string) => (b: string): string => a + b))(needle))(((((a: string) => (b: string): string => a + b))(`\n  got: `))(got)))))));

function main(): void {
  ((src1) => ((out1) => ((_) => ((_) => ((_) => ((src2) => ((out2) => ((_) => ((_) => ((_) => ((_) => ((_) => ((src3) => ((out3) => ((_) => ((src4) => ((out4) => ((_) => ((_) => ((src5) => ((out5) => ((_) => ((_) => ((chk6) => ((_) => ((_) => ((src7) => ((out7) => ((_) => ((_) => ((tok8) => ((_) => ((_) => 0)((((compilerCheckContains)(`8 tokens has non layout`))(tok8))(`"non_layout_tokens":`)))((((compilerCheckContains)(`8 tokens has bytes`))(tok8))(`"source_bytes":`)))((tokenEstimate)(src1)))((((compilerCheckContains)(`7 render fn`))(out7))(`add(a Int)(b Int) = (a + b)`)))((((compilerCheckContains)(`7 render module`))(out7))(`module Test.Render`)))((renderCompact)(src7)))(`module Test.Render\nadd(a Int)(b Int) = a + b\n`))((((compilerCheckContains)(`6 compact primary error`))(chk6))(`"primary_error":"Unbound variable: y"`)))((((compilerCheckContains)(`6 compact stage`))(chk6))(`"stage":"elaborate"`)))((checkCompact)(src3)))((((compilerCheckContains)(`5 if expression`))(out5))(`if`)))((((compilerCheckContains)(`5 abs function`))(out5))(`let rec abs`)))((compile)(src5)))(`module Test.IfElse\nabs(n Int) = if n < 0\n  0 - n\nelse n\n`))((((compilerCheckContains)(`4 let binding`))(out4))(`let answer`)))((((compilerCheckContains)(`4 module const`))(out4))(`module Test.Const`)))((compile)(src4)))(`module Test.Const\nlet answer = 42\n`))((((checkHasError)(`3 unbound var error`))(out3))(`Unbound variable: y`)))((compile)(src3)))(`module Test.Bad\nbroken(x Int) = y\n`))((((compilerCheckContains)(`2 match expr`))(out2))(`match`)))((((compilerCheckContains)(`2 describe fn`))(out2))(`let rec describe`)))((((compilerCheckContains)(`2 ctor Red`))(out2))(`| Red`)))((((compilerCheckContains)(`2 type Color`))(out2))(`type Color`)))((((compilerCheckContains)(`2 module colors`))(out2))(`module Test.Colors`)))((compile)(src2)))(`module Test.Colors\nColor = Red | Blue | Green\ndescribe(c Color) =\n  match c\n    | Red -> "red"\n    | Blue -> "blue"\n    | Green -> "green"\n`))((((compilerCheckContains)(`1 add function`))(out1))(`let rec add`)))((((compilerCheckContains)(`1 prelude present`))(out1))(`ll-lang stdlib prelude`)))((((compilerCheckContains)(`1 module header`))(out1))(`module Test.Hello`)))((compile)(src1)))(`module Test.Hello\nadd(a Int)(b Int) = a + b\n`);
}


main();