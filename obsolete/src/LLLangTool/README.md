# Obsolete MCP Wrapper Archive

This directory contains archived legacy F# MCP wrapper code that is **not** part of active build/runtime.

- Source archived from commit parent of `e34e1f1`:
  - `src/LLLangTool/Mcp.fs`
- Current active MCP implementation is self-hosted ll-lang:
  - `lllcself/src/Mcp.lll`

Do not add files from this folder to any `*.fsproj` compile items.
