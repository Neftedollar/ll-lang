# NEXT_STEPS

## 2026-04-19 migration bundle (completed)
- [x] Bootstrap artifact + lock automation (`tools/build-bootstrap-artifacts.sh` + `bootstrap-release.yml`)
- [x] Self-host CLI e2e script (`tools/check-selfhost-e2e.sh`) wired into `build.yml`
- [x] LLVM smoke chain (`tools/check-llvm-smoke.sh`) wired into `build.yml`
- [x] Archive stage0 under `obsolete/stage0` and demote legacy workflow to manual-only

## MCP (LLL-first) stabilization
- [x] Sync docs with real MCP surface (`lllc mcp` now served by `lllcself`):
  - [x] `README.md` (tool inventory + architecture note)
  - [x] `docs/user-guide/09-mcp.md`
  - [x] `docs/compiler-dev/10-mcp-server.md`
- [x] Add MCP contract tests for the current self-hosted tool set:
  - [x] `initialize`
  - [x] `tools/list`
  - [x] `tools/call` smoke for `#159..#168` additions (`diagnose_*`, `format_*`, `parse_source`, `typed_ast`, `project_graph`, `symbols/definition/references`, `explain_error`, `fix_*`, `check_project/build_project`, `test_*`, `mod_*`)
- [x] Remove stale references to deleted `src/LLLangTool/Mcp.fs`.

## Self-hosting path (toward full LLL toolchain)
- [x] Define canonical plan to switch default compile/check/run path from stage0 F# to self-hosted pipeline.
  - [x] Added cutover doc: `docs/compiler-dev/23-selfhost-default-cutover-plan.md` (issue #175).
- [x] Add parity gate: same diagnostics and output between stage0 and self-hosted for corpus.
  - [x] Added `tools/check-stage0-self-parity.sh` + CI gate step (issue #176).
- [x] Introduce rollout flag strategy (safe cutover, fallback only for blockers).
  - [x] Added launcher routing flags in `tools/lllc-bootstrap.sh` + CI safe preset smoke (issue #175).

## FFI and MCP roadmap coupling
- [ ] Finish FFI evolution in `.lll`-owned flow (no new feature logic in F#).
- [ ] Expose FFI inspection/debug helpers via MCP tools for LLM workflows.
- [ ] Add docs section: “FFI on LLL path only”.

## Language ergonomics for compiler-writing
- [x] Evaluate built-in operator set (`|>`, `>>=`, `<|>`, `>>`) and lock precedence/associativity. (#178)
- [x] Add parser parity tests for new surface syntax. (#178)
- [x] Add TODO markers with fixed categories where blocked:
  - [x] `TODO(selfhost:operators)`
  - [x] `TODO(selfhost:typing)`
  - [x] `TODO(selfhost:resolver)`
  - [x] `TODO(selfhost:backend)`

## Release readiness checkpoints
- [ ] Full test suite green (no ignored critical paths except explicitly documented reverse tests).
- [ ] Self-hosted compiler builds at least one real multi-module project with dependencies.
- [ ] Update release docs with a factual “what is still stage0-only” matrix.
